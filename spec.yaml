id: mara_north_event_report
requirements:
  - name: ecoscope-platform
    version: ">=2.15.0, <2.16.0"
    channel: https://repo.prefix.dev/ecoscope-workflows/

  - name: ecoscope-workflows-ext-custom
    version: "0.1.0rc14.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: ecoscope-workflows-ext-ste
    version: "0.0.0rc1.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: ecoscope-workflows-ext-wwf-virunga
    version: "0.0.0rc9.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: ecoscope-workflows-ext-big-life
    version: "1.0.1.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: ecoscope-workflows-ext-mnc
    version: "1.0.5.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/
    
  - name: pydeck
    version: 0.9.2
    channel: conda-forge

  - name: opentelemetry-sdk
    version: ">=1.20.0,<2.0.0"
    channel: conda-forge

task-instance-defaults:
  skipif:
    conditions: 
      - any_is_empty_df 
      - any_dependency_skipped 
      
workflow:
  - name: Workflow Details
    id: workflow_details
    task: set_workflow_details

  - name: Time Range
    id: time_range
    task: set_time_range
      
  - name: Set Groupers
    id: groupers
    task: set_groupers
    partial: 
      groupers: []

  - name: Connect to EarthRanger
    id: er_client_name
    task: set_er_connection

  - name: Configure base map layers
    id: configure_base_maps
    task: set_base_maps_pydeck
    partial:
      base_maps:
        - url: >-
            https://server.arcgisonline.com/arcgis/rest/services/Elevation/World_Hillshade/MapServer/tile/{z}/{y}/{x}
          opacity: 0.80
          max_zoom: 20

        - url: https://services.arcgisonline.com/ArcGIS/rest/services/Reference/World_Boundaries_and_Places_Alternate/MapServer/tile/{z}/{y}/{x}
          opacity: 0.35
          max_zoom: 20

  - name: Download MNC community conservancy boundaries
    id: persist_mnc_gpkg
    task: ecoscope_workflows_ext_ste.tasks.io.fetch_and_persist_file
    partial:
      url: https://www.dropbox.com/scl/fi/14rcy4lkwp7xgewj3xf7k/mnc_conservancy.gpkg?rlkey=mtqo7ivxrnvjonm2z1zez6h6f&st=gtdi4vv0&dl=0
      output_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      overwrite_existing: false
      retries: 3
      unzip: false
  
  - name: Download MNC Parcels gpkg
    id: download_mnc_parcels
    task: ecoscope_workflows_ext_ste.tasks.io.fetch_and_persist_file
    partial:
      url: https://www.dropbox.com/scl/fi/33rdzy896rh91gtkfs2j3/mnc_across_the_river_parcels.gpkg?rlkey=xima1v0rozc0h9h78esfogx6q&st=o3gkk5p1&dl=0
      output_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      overwrite_existing: false
      retries: 3
      unzip: false
  
  - name : Load community conservancy boundaries
    id: load_comm_shp
    task: load_df
    partial:
      file_path: ${{ workflow.persist_mnc_gpkg.return }}
      layer: null
      deserialize_json: false

  - name: Fix invalid geometries in community boundaries
    id: fix_comm_geom
    task: ecoscope_workflows_ext_mnc.tasks.transformation.fix_invalid_geometries
    partial:
      gdf: ${{ workflow.load_comm_shp.return }}

  - name: Filter conservancy boundary only
    id: filter_conservancy_boundary
    task: filter_df
    partial:
      df: ${{ workflow.fix_comm_geom.return }}
      column_name: grazing_zone
      op: equal
      value: Conservancy
      reset_index: true

  - name: Filter to Mara North Conservancy
    id: filter_mara_north
    task: filter_df
    partial:
      df: ${{ workflow.fix_comm_geom.return }}
      column_name: name
      op: equal
      value: Mara North Conservancy
      reset_index: true

  - name: Filter grazing zones excluding conservancy
    id: filter_grazing_zones
    task: filter_df
    partial:
      df: ${{ workflow.fix_comm_geom.return }}
      column_name: grazing_zone
      op: ne
      value: Conservancy
      reset_index: true

  - name: Color grazing zones by unique value
    id: color_grazing_zones
    task: apply_color_map
    partial:
      df: ${{ workflow.filter_grazing_zones.return }}
      input_column_name: grazing_zone
      output_column_name: zone_color
      colormap: GnBu #Greens

  - name: Build grazing zones legend values
    id: grazing_zone_legend_values
    task: ecoscope_workflows_ext_mnc.tasks.transformation.build_legend_values_from_column
    partial:
      df: ${{ workflow.color_grazing_zones.return }}
      label_column: grazing_zone
      color_column: zone_color
      sort: ascending

  - name: Load MNC parcels
    id: load_mnc_parcels
    task: load_df
    partial:
      file_path: ${{ workflow.download_mnc_parcels.return }}
      layer: null
      deserialize_json: false

  - name: Create conservancy boundary layer
    id: create_conservancy_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      geodataframe: ${{ workflow.filter_conservancy_boundary.return }}
      data_url: null
      layer_style:
        filled: false
        stroked: true
        get_line_color: [169, 169, 169, 255]
        get_line_width: 1.75
      legend:
        title: ""
        values:
          - label: Conservancy Boundary
            color: "#a9a9a9"
        sort: null
        label_suffix: null

  - name: Create grazing zones layer
    id: create_grazing_zones_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      geodataframe: ${{ workflow.color_grazing_zones.return }}
      data_url: null
      layer_style:
        filled: true
        stroked: true
        opacity: 0.5
        get_fill_color: zone_color
        get_line_color: zone_color
        get_line_width: 1.5
      legend:
        title: ""
        values: ${{ workflow.grazing_zone_legend_values.return }}

  - name: Create parcels layer
    id: create_parcels_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      geodataframe: ${{ workflow.load_mnc_parcels.return }}
      data_url: null
      layer_style:
        filled: true
        stroked: true
        get_fill_color: [189, 183, 107, 60]
        get_line_color: [189, 183, 107, 255]
        get_line_width: 1.5
      legend:
        title: "Map Layers"
        values:
          - label: Parcels
            color: "#bdb76b"
        sort: null
        label_suffix: null

  #1. Weather Report 
    # 1. --- Weather report --- 
  - name: Retrieve weather observations from GMMF 
    id: subject_observations
    task: get_subjectgroup_observations
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      filter: clean
      client: ${{ workflow.er_client_name.return }}
      time_range: ${{ workflow.time_range.return }}
      raise_on_empty: false
      include_details: true
      include_subjectsource_details: true
      subject_group_name: "ER2ER - From GMMF"
  
  # Extract precipitation data
  - name: Extract precipitation data
    id: extract_precipitation
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.subject_observations.return }}
      column_name: extra__observation_details
      field_name_options:
        - precipitation
      output_type: float
      output_column_name: precipitation
  
  # Extract temperature data
  - name: Extract temperature data
    id: extract_temperature
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_precipitation.return }}
      column_name: extra__observation_details
      field_name_options:
        - surface_air_temperature
      output_type: float
      output_column_name: temperature
  
  # Extract wind speed data 
  - name: Extract wind speed data
    id: extract_wind_speed
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_temperature.return }}
      column_name: extra__observation_details
      field_name_options:
        - wind_speed
      output_type: float
      output_column_name: wind_speed

  # Extract wind gusts data 
  - name: Extract wind gusts data
    id: extract_wind_gusts
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_wind_speed.return }}
      column_name: extra__observation_details
      field_name_options:
        - wind_gusts
      output_type: float
      output_column_name: wind_gusts

    # Extract soil temperature data 
  - name: Extract soil temperature data
    id: extract_soil_temperature
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_wind_gusts.return }}
      column_name: extra__observation_details
      field_name_options:
        - soil_temperature
      output_type: float
      output_column_name: soil_temperature

    # Extract relative humidity data 
  - name: Extract relative humidity data
    id: extract_relative_humidity
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_soil_temperature.return }}
      column_name: extra__observation_details
      field_name_options:
        - relative_humidity
      output_type: float
      output_column_name: relative_humidity

    # Extract atmospheric pressure data
  - name: Extract atmospheric pressure data
    id: extract_pressure
    task: extract_value_from_json_column
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_relative_humidity.return }}
      column_name: extra__observation_details
      field_name_options:
        - atmospheric_pressure
      output_type: float
      output_column_name: atmospheric_pressure

  # Extract date
  - name: Extract date
    id: extract_date
    task: extract_column_as_type
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.extract_pressure.return }}
      column_name: fixtime
      output_type: date
      output_column_name: date
  
  # Rename column
  - name: Rename value grouper columns
    id: rename_grouper_columns
    task: map_columns
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      raise_if_not_found: true
      df: ${{ workflow.extract_date.return }}
      drop_columns: []
      retain_columns: []
      rename_columns:
        extra__subject__name: "weather_station"

  - name: Add temporal index
    id: df_with_temporal_index
    task: add_temporal_index
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.rename_grouper_columns.return }}
      time_col: fixtime
      cast_to_datetime: true
      groupers: ${{ workflow.groupers.return }}
      format: mixed 

  - name: Calculate weather summary table 
    id: daily_weather
    task: summarize_df
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      df: ${{ workflow.df_with_temporal_index.return }}
      groupby_cols:
        - weather_station
        - date
      summary_params:
        - display_name: precipitation
          aggregator: sum
          column: precipitation

        - display_name: temperature
          aggregator: mean
          column: temperature

        - display_name: wind_speed
          aggregator: mean
          column: wind_speed
        
        - display_name: wind_gusts
          aggregator: max
          column: wind_gusts
        
        - display_name: soil_temperature
          aggregator: mean 
          column: soil_temperature

        - display_name: relative_humidity
          aggregator: mean 
          column: relative_humidity

        - display_name: atmospheric_pressure
          aggregator: mean 
          column: atmospheric_pressure

      reset_index: true

  - name: Perist weather summary table 
    id: persist_weather_summary 
    task: persist_df
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:  
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.daily_weather.return }}
      filename: weather_summary_table

  # Draw weather charts and persist
  # Draw precipitation chart and persist
  - name: Draw precipitation chart
    id: precipitation_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: precipitation
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          showgrid: true
          gridcolor: "#e0e0e0"
          gridwidth: 1
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Precipitation (mm)"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist precipitation chart as text
    id: persist_precipitation
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.precipitation_chart.return }}
      filename: precipitation_readings_over_time.html

  - name: Create precipitation plot widget
    id: precipitation_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Precipitation"
      data: ${{ workflow.persist_precipitation.return }}

  # Draw temperature chart ,persist
  - name: Draw temperature chart
    id: temperature_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: temperature
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Average Temperature (°C)"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist temperature chart as text
    id: persist_temperature
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.temperature_chart.return }}
      filename: temperature_readings_over_time.html

  - name: Create temperature plot widget
    id: temperature_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Temperature"
      data: ${{ workflow.persist_temperature.return }}

  # Draw wind speed  chart, persist and add widgets
  - name: Draw wind speed chart 
    id: wind_speed_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: wind_speed
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Average Wind Speed"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist wind speed chart as text
    id: persist_wind_speed
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.wind_speed_chart.return }}
      filename: wind_speed_readings_over_time.html

  - name: Create wind speed plot widget
    id: wind_speed_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Wind Speed"
      data: ${{ workflow.persist_wind_speed.return }}

  # Draw wind gusts  chart, persist and add widgets
  - name: Draw wind gusts chart 
    id: wind_gusts_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: wind_gusts
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Max Wind Gusts"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist wind gusts chart as text
    id: persist_wind_gusts
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.wind_gusts_chart.return }}
      filename: wind_gusts_readings_over_time.html

  - name: Create wind gusts plot widget
    id: wind_gusts_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Wind Gusts"
      data: ${{ workflow.persist_wind_gusts.return }}

  # Draw soil temperature chart, persist and add widgets
  - name: Draw soil temperature chart 
    id: soil_temp_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: soil_temperature
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Average Temperature (°C)"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist soil temperature chart as text
    id: persist_soil_temp
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.soil_temp_chart.return }}
      filename: soil_temperature_readings_over_time.html

  - name: Create soil temperature plot widget
    id: soil_temp_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Soil Temperature"
      data: ${{ workflow.persist_soil_temp.return }}

  # Draw relative humidity and persist
  - name: Draw relative humidity chart
    id: rel_humidity_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: relative_humidity
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Average Humidity"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist relative humidity chart as text
    id: persist_rel_humidity
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.rel_humidity_chart.return }}
      filename: relative_humidity_readings_over_time.html

  - name: Create relative humidity plot widget
    id: rel_humidity_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Relative Humidity"
      data: ${{ workflow.persist_rel_humidity.return }}

  # Draw atmospheric pressure and persist
  - name: Draw atmospheric pressure chart
    id: pressure_chart
    task: ecoscope.platform.tasks.results.draw_line_chart
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      smoothing: null
      dataframe: ${{ workflow.daily_weather.return }}
      x_column: date
      y_column: atmospheric_pressure
      category_column: weather_station
      line_kwargs:
        shape: "linear"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: true
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Average Pressure"
          tickformat: ".1f"
        legend_title: "Weather Station"
        hovermode: "x unified"

  - name: Persist atmospheric pressure chart as text
    id: persist_pressure
    task: persist_text
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.pressure_chart.return }}
      filename: atmospheric_pressure_readings_over_time.html

  - name: Create atmospheric pressure plot widget
    id: pressure_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Atmospheric Pressure"
      data: ${{ workflow.persist_pressure.return }}

  - name: Convert chart htmls to png
    id: convert_chart_html_png 
    task: html_to_png 
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial: 
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: 
        - ${{ workflow.persist_precipitation.return }} # precipitation
        - ${{ workflow.persist_temperature.return }} # temperature 
        - ${{ workflow.persist_wind_speed.return }} # wind speed
        - ${{ workflow.persist_wind_gusts.return }} # wind gusts 
        - ${{ workflow.persist_soil_temp.return }} # soil temperature 
        - ${{ workflow.persist_rel_humidity.return }} # relative humidity
        - ${{ workflow.persist_pressure.return }} # atmospheric pressure

      config: 
        width: 1280          
        height: 720          
        full_page: false
        device_scale_factor: 2.0 
        wait_for_timeout: 1
        timeout: 0 
        max_concurrent_pages: 10

  # 2. Logistics Report 
  - name: Retrieve All Events 
    id: get_events_data
    task: get_events
    partial:
      client: ${{ workflow.er_client_name.return }}
      time_range: ${{ workflow.time_range.return }}
      event_columns:
        - id
        - time
        - event_type
        - event_category
        - reported_by
        - serial_number
        - geometry
        - created_at
        - event_details
        - patrols
      event_types: []
      raise_on_empty: true
      include_details: true
      include_updates: false
      include_related_events: false
      include_null_geometry: false
      include_display_values: false
      force_point_geometry: true 

  # Extract date
  - name: Extract date from events data
    id: extract_event_date
    task: extract_column_as_type
    partial:
      df: ${{ workflow.get_events_data.return }}
      column_name: time
      output_type: date
      output_column_name: date

  - name: Add temporal index on events
    id: events_temporal
    task: add_temporal_index
    partial:
      df: ${{ workflow.extract_event_date.return }}
      time_col: date
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true
      format: mixed

  - name: Filter events and get balloon sighting records 
    id: filter_balloon_events
    task: filter_df
    partial:
      column_name: event_type	
      op: equal
      value: balloon_landing
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Filter events and get airstrip operation records 
    id: filter_airstrip_operations
    task: filter_df
    partial:
      column_name: event_type	
      op: equal
      value: airstrip_operations
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Filter events and get airstrip maintenance records 
    id: filter_airstrip_maintenance
    task: filter_df
    partial:
      column_name: event_type	
      op: equal
      value: airstrip_maintenance
      df: ${{ workflow.events_temporal.return }}
      reset_index: false
  
  - name: Filter events and get airline complaint records 
    id: filter_airline_complaints
    task: filter_df
    partial:
      column_name: event_type	
      op: equal
      value: airline_complaint
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process balloon event details 
    id: process_balloon_details
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_balloon_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true 

  - name: Normalize balloon event details values
    id: normalize_balloon_values
    task: normalize_json_column
    partial:
      column: event_details
      df: ${{ workflow.process_balloon_details.return }}
      skip_if_not_exists: true 
      sort_columns: true 
  
  - name: Drop column prefix from balloon event details s
    id: drop_balloon_prefix 
    task: drop_column_prefix 
    partial:
      df: ${{ workflow.normalize_balloon_values.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"
  
  - name: Process airstrip operation event details 
    id: process_airstrip_details
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_airstrip_operations.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true
  
  - name: Normalize airstrip operation event details values
    id: normalize_airstrip_op_values
    task: normalize_json_column
    partial: 
      column: event_details
      df: ${{ workflow.process_airstrip_details.return }}
      skip_if_not_exists: true 
      sort_columns: true
    
  - name: Drop column prefix from airstrip operation event details 
    id: drop_airstrip_op_prefix 
    task: drop_column_prefix 
    partial:
      df: ${{ workflow.normalize_airstrip_op_values.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Process airstrip maintenance event details 
    id: process_airstrip_maint_details
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_airstrip_maintenance.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true
  
  - name: Normalize airstrip maintenance event details values
    id: normalize_airstrip_maint_vals
    task: normalize_json_column
    partial: 
      column: event_details
      df: ${{ workflow.process_airstrip_maint_details.return }}
      skip_if_not_exists: true 
      sort_columns: true
  
  - name: Drop column prefix from airstrip maintenance event details 
    id: drop_airstrip_maintenance_prefix 
    task: drop_column_prefix 
    partial:
      df: ${{ workflow.normalize_airstrip_maint_vals.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Process airline complaint event details 
    id: process_airline_comp_details
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_airline_complaints.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true
  
  - name: Normalize airline complaint event details values
    id: normalize_airline_comp_vals
    task: normalize_json_column
    partial: 
      column: event_details
      df: ${{ workflow.process_airline_comp_details.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop column prefix from airline complaint event details 
    id: drop_airline_complaint_prefix 
    task: drop_column_prefix 
    partial:
      df: ${{ workflow.normalize_airline_comp_vals.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map balloon columns 
    id: map_balloon_columns 
    task: map_columns
    partial: 
      df: ${{ workflow.drop_balloon_prefix.return }}
      drop_columns: []
      retain_columns: [
        "date",
        "Balloon Company",
        "Where are clients staying?",
        "# of passengers"
      ]
      rename_columns: {
        "Balloon Company": "balloon_company",
        "Where are clients staying?": "where_are_clients_staying",
        "# of passengers":"no_of_passengers"
        }
      raise_if_not_found: false

  - name: Remove brackets from columns in balloon events
    id: remove_balloon_brackets
    task: ecoscope_workflows_ext_mnc.tasks.transformation.remove_brackets_from_column

    partial:
      df: ${{ workflow.map_balloon_columns.return }}
      columns:
        - balloon_company
        - where_are_clients_staying

  - name: Rename balloon landing table columns for display
    id: rename_balloon_display
    task: map_columns
    partial:
      df: ${{ workflow.remove_balloon_brackets.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "date": "Date",
        "balloon_company": "Balloon Company",
        "where_are_clients_staying": "Where Are Clients Staying",
        "no_of_passengers": "No Of Passengers"
        }
      raise_if_not_found: false

  - name: Persist balloon landing summary table
    id: persist_balloon_landing
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_balloon_display.return }}
      filename: balloon_landing_summary_table

  - name: Draw balloon landing summary table
    id: balloon_landing_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_balloon_display.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false
      widget_id: "Balloon Landing Summary"

  - name: Save balloon landing summary table (HTML)
    id: balloon_landing_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: balloon_landing_summary_table.html
      text: ${{ workflow.balloon_landing_table_html.return }}

  - name: Create balloon landing summary table widget
    id: balloon_landing_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Balloon Landing Summary"
      data: ${{ workflow.balloon_landing_table_html_url.return }}

  # map airstrip operations
  - name: Map airstrip operation columns 
    id: map_airstrip_op_columns 
    task: map_columns
    partial: 
      df: ${{ workflow.drop_airstrip_op_prefix.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "Airline":"airline",
        "Arrival or departure":"arrival_departure",
        "Attendant":"attendant",
        "Camp/Lodge":"camp_lodge",
        "Number of clients":"no_of_clients"
        }
      raise_if_not_found: false

  - name: Remove brackets from columns in airstrip operation events
    id: remove_airstrip_op_brackets
    task: ecoscope_workflows_ext_mnc.tasks.transformation.remove_brackets_from_column
    partial:
      df: ${{ workflow.map_airstrip_op_columns.return }}
      columns:
        - airline
        - arrival_departure
        - attendant
        - camp_lodge

  - name: Convert columns to integer 
    id: convert_airstrip_op_ints
    task: ecoscope_workflows_ext_mnc.tasks.transformation.convert_columns_to_int
    partial:
      df: ${{ workflow.remove_airstrip_op_brackets.return }} 
      columns:
        - no_of_clients
      errors: coerce
      fill_value: 0

  - name: Airstrip operation events summary table
    id: airstrip_op_summary_table 
    task: summarize_df 
    partial:
      df: ${{ workflow.convert_airstrip_op_ints.return }}
      groupby_cols:
        - camp_lodge
        - arrival_departure
      summary_params:
        - display_name: no_of_clients 
          aggregator: sum 
          column: no_of_clients
          decimal_places: 0 
      reset_index: true

  - name: Pivot airstrip operations summary table 
    id: pivot_airstrip_ops
    task: pivot_dataframe
    partial: 
      df: ${{ workflow.airstrip_op_summary_table.return }}
      index: camp_lodge
      columns: 
        - arrival_departure 
      values: 
        - no_of_clients
      fill_value: 0
  
  - name: Convert airstrip pivot table values to int
    id: convert_pivot_int
    task: ecoscope_workflows_ext_mnc.tasks.transformation.convert_columns_to_int
    partial:
      df: ${{ workflow.pivot_airstrip_ops.return }}
      columns: 
        - arrival
        - departure 
      errors: coerce 
      fill_value: 0 
      
  - name: Rename airstrip operations table columns for display
    id: rename_airstrip_op_display
    task: map_columns
    partial:
      df: ${{ workflow.convert_pivot_int.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "camp_lodge": "Camp Lodge",
        "no_of_clients_Arrival": "Arrival",
        "no_of_clients_Departure": "Departure"
        }
      raise_if_not_found: false

  - name: Persist airstrip operations summary table
    id: persist_airstrip_operations
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_airstrip_op_display.return }}
      filename: airstrip_operations_summary_table

  - name: Draw airstrip operations summary table
    id: airstrip_op_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_airstrip_op_display.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false
      widget_id: "Airstrip Operations Summary"

  - name: Save airstrip operations summary table (HTML)
    id: airstrip_op_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: airstrip_operations_summary_table.html
      text: ${{ workflow.airstrip_op_table_html.return }}

  - name: Create airstrip operations summary table widget
    id: airstrip_op_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Airstrip Operations Summary"
      data: ${{ workflow.airstrip_op_table_html_url.return }}

  - name: Map airstrip maintenace columns
    id: map_airstrip_maintenance
    task: map_columns 
    partial:
      df: ${{ workflow.drop_airstrip_maintenance_prefix.return }}
      drop_columns: []
      retain_columns: [
        "date",
        "Maintenance type"
      ]
      rename_columns: {
        "Maintenance type":"maintenance_type"
        }
      raise_if_not_found: false

  - name: Rename airstrip maintenance table columns for display
    id: rename_airstrip_maint_display
    task: map_columns
    partial:
      df: ${{ workflow.map_airstrip_maintenance.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "date": "Date",
        "maintenance_type": "Maintenance Type"
        }
      raise_if_not_found: false

  - name: Persist airstrip maintenance summary table
    id: persist_airstrip_maintenance
    task: persist_df

    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_airstrip_maint_display.return }}
      filename: airstrip_maintenance_summary_table

  - name: Draw airstrip maintenance summary table
    id: airstrip_maintenance_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_airstrip_maint_display.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false
      widget_id: "Airstrip Maintenance Summary"

  - name: Save airstrip maintenance summary table (HTML)
    id: airstrip_maint_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename_suffix: airstrip_maintenance_summary_table.html
      text: ${{ workflow.airstrip_maintenance_table_html.return }}

  - name: Create airstrip maintenance summary table widget
    id: airstrip_maint_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Airstrip Maintenance Summary"
      data: ${{ workflow.airstrip_maint_table_html_url.return }}

  #3. Livestock Report 
  - name: Retrieve mobile boma events only 
    id: filter_mobile_boma 
    task: filter_df 
    partial: 
      df: ${{ workflow.events_temporal.return }}
      column_name: event_type
      op: equal 
      value: mobile_boma_rep 
      reset_index: true 

  - name: Retrieve livestock predation events only 
    id: filter_livestock_predation 
    task: filter_df 
    partial: 
      df: ${{ workflow.events_temporal.return }}
      column_name: event_type
      op: equal 
      value: livestock_predation_rep
      reset_index: true 

  - name: Filter cattle count events only 
    id: filter_cattle_counts 
    task: filter_df 
    partial: 
      df: ${{ workflow.events_temporal.return }}
      column_name: event_type
      op: equal 
      value: cattle_count  
      reset_index: true 
  
  - name: Filter out illegal grazing events only 
    id: filter_illegal_grazing 
    task: filter_df 
    partial: 
      df: ${{ workflow.events_temporal.return }}
      column_name: event_type
      op: equal 
      value: illegal_grazing_rep 
      reset_index: true 
  
  - name: Process mobile boma event details 
    id: process_mobile_boma 
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_mobile_boma.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize mobile boma events 
    id: normalize_mobile_boma 
    task: normalize_json_column
    partial:
      df: ${{ workflow.process_mobile_boma.return }}
      column: event_details 
      skip_if_not_exists: true 
      sort_columns: true 
  
  - name: Drop mobile boma column prefix 
    id: drop_mobile_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_mobile_boma.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Process livestock predation event details 
    id: process_livestock_preds 
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_livestock_predation.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize livestock predation events 
    id: normalize_livestock_preds
    task: normalize_json_column
    partial:
      df: ${{ workflow.process_livestock_preds.return }}
      column: event_details 
      skip_if_not_exists: true 
      sort_columns: true 
  
  - name: Drop livestock predation column prefix 
    id: drop_predation_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_livestock_preds.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Process cattle count event details 
    id: process_cattle_counts 
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_cattle_counts.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize cattle count events 
    id: normalize_cattle_count
    task: normalize_json_column
    partial:
      df: ${{ workflow.process_cattle_counts.return }}
      column: event_details 
      skip_if_not_exists: true 
      sort_columns: true 
  
  - name: Drop cattle count column prefix 
    id: drop_cattle_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_cattle_count.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"
  
  - name: Process illegal grazing event details 
    id: process_illegal_grazing
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_illegal_grazing.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize illegal grazing events 
    id: normalize_illegal_grazing
    task: normalize_json_column
    partial:
      df: ${{ workflow.process_illegal_grazing.return }}
      column: event_details 
      skip_if_not_exists: true 
      sort_columns: true 
  
  - name: Drop illegal grazing column prefix 
    id: drop_illegal_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_illegal_grazing.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"
  
  - name: Map mobile boma columns 
    id: map_mobile_boma 
    task: map_columns 
    partial: 
      df: ${{ workflow.drop_mobile_prefix.return }}
      drop_columns: []
      retain_columns: [
        "id",
        "date",
        "event_type",
        "geometry",
        "Date of Relocation",
        "Electric Boma Status",
        "Mobile Boma Zone",
        "Nature of the Site",
        "Reason for relocation",
        ]
      rename_columns: {}
      raise_if_not_found: false 
  
  - name: Summarize mobile boma events 
    id: summarize_mobile_boma 
    task: summarize_df 
    partial: 
      df: ${{ workflow.map_mobile_boma.return }}
      groupby_cols:
        - date 
      summary_params: 
        - display_name: boma_events 
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true 
  
  - name: Persist mobile boma summary table
    id: persist_boma_summary
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.summarize_mobile_boma.return }}
      filename: mobile_boma_movement_summary_table 

  # Draw mobile boma map 
  - name: Drop null geometry from mobile boma 
    id: drop_null_mobile
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.map_mobile_boma.return }} 
      geometry_column: "geometry"

  - name: Generate mobile boma point layers
    id: generate_mobile_layers
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      data_url: null
      layer_style:
        get_fill_color: [0, 0, 128]
        get_line_color: [0, 0, 128]
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Movements
        values: 
          - label: Boma movement
            color: "#000080"
        sort: null
        label_suffix: null
      geodataframe: ${{ workflow.drop_null_mobile.return }} 
  
  - title: Map Zoom & Extent
    type: task-group
    description: Calculate the geographic extent of the movement tracks and determine an appropriate zoom level and center point for the map. This ensures that all tracks are visible and well-framed when the map is rendered.
    tasks:
      - name: null
        id: zoom_to_envelope
        task: ecoscope_workflows_ext_ste.tasks.spatial_operations.envelope_gdf
        partial:
          gdf: ${{ workflow.filter_mara_north.return }}

      - name: Zoom view to gdf extent
        id: gdf_image_extent
        task: ecoscope_workflows_ext_ste.tasks.spatial_operations.compute_view_state_from_gdf
        partial:
          pitch: 0
          bearing: 0
          max_zoom: 15
          gdf: ${{ workflow.zoom_to_envelope.return }}

  - name: Combine styled layers with mobile boma
    id: combine_custom_mobile_boma
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_grazing_zones_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_mobile_layers.return }}

  - name: Draw mobile boma events map
    id: draw_mobile_boma
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_mobile_boma.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist mobile boma map HTML paths
    id: persist_mobile_boma_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_mobile_boma.return }}
      filename: boma_movement_map.html
      filename_suffix: null

  - name: Create mobile boma map widget
    id: mobile_boma_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Mobile Boma Movement Map"
      data: ${{ workflow.persist_mobile_boma_urls.return }}

  - name: Convert mobile boma map to png
    id: convert_mobile_boma_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_mobile_boma_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1
    
  - name: Map cattle count columns 
    id: map_cattle_count 
    task: map_columns
    partial:
      df: ${{ workflow.drop_cattle_prefix.return }}
      drop_columns: [
        "total_cattle_counted_from_all_zones"
        ]
      retain_columns: [
        "date",
        "# cattle in Zone 1 mobile boma",
        "# cattle in Zone 2/3 mobile boma",
        "# cattle in Zone 4",

        ]
      rename_columns: {
        "# cattle in Zone 1 mobile boma": "zone_1",
        "# cattle in Zone 2/3 mobile boma": "zone_2_3",
        "# cattle in Zone 4":"zone_4",
        }
      raise_if_not_found: false


  - name: Convert all values to numeric 
    id: convert_cols_numeric
    task: convert_column_values_to_numeric
    partial: 
      df: ${{ workflow.map_cattle_count.return }}
      columns: 
        - "zone_1"
        - "zone_2_3"
        - "zone_4"
  
  - name: Compute total count on all zone columns 
    id: compute_total_count 
    task: ecoscope_workflows_ext_mnc.tasks.aggregation.apply_arithmetic_operation_over_rows
    partial:  
      df: ${{ workflow.convert_cols_numeric.return }}
      columns: 
        - "zone_1"
        - "zone_2_3"
        - "zone_4"
      operation: add
      output_column: total 

  - name: Rename cattle count table columns for display
    id: rename_cattle_count_display
    task: map_columns
    partial:
      df: ${{ workflow.compute_total_count.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "date": "Date",
        "zone_1": "Zone 1",
        "zone_2_3": "Zone 2/3",
        "zone_4": "Zone 4",
        "total": "Total"
        }
      raise_if_not_found: false

  - name: Persist cattle count table
    id: persist_cattle_count
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_cattle_count_display.return }}
      filename: total_cattle_count_summary_table

  - name: Draw cattle count summary table
    id: cattle_count_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_cattle_count_display.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false
      widget_id: "Total Cattle Count Summary"

  - name: Save cattle count summary table (HTML)
    id: cattle_count_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.cattle_count_table_html.return }}
      filename: total_cattle_count_summary_table.html
      filename_suffix: null

  - name: Create cattle count summary table widget
    id: cattle_count_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Total Cattle Count Summary"
      data: ${{ workflow.cattle_count_table_html_url.return }}

  - name: Map livestock predation events 
    id: map_livestock_predation 
    task: map_columns
    partial: 
      df: ${{ workflow.drop_predation_prefix.return }}
      drop_columns: [] 
      retain_columns: [
        "id",
        "date",
        "event_type",
        "geometry",
        "Livestock Species",
        "Suspected Predator",
        "Total livestock affected"
        ]
      rename_columns: {}
      raise_if_not_found: false 
  
  - name: Summarize livestock predation events 
    id: summarize_predation_events 
    task: summarize_df 
    partial: 
      df: ${{ workflow.map_livestock_predation.return }}
      groupby_cols:
        - date 
      summary_params: 
        - display_name: livestock_predation_events 
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true 

  - name: Rename livestock predation summary table columns for display
    id: rename_predation_summary_display
    task: map_columns
    partial:
      df: ${{ workflow.summarize_predation_events.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "date": "Date",
        "livestock_predation_events": "Livestock Predation Events"
        }
      raise_if_not_found: false

  - name: Persist livestock predation summary table
    id: persist_predation_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_predation_summary_display.return }}
      filename: total_livestock_predation_summary_table

  - name: Draw livestock predation summary table
    id: predation_summary_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_predation_summary_display.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false
      widget_id: "Livestock Predation Summary"

  - name: Save livestock predation summary table (HTML)
    id: predation_summary_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.predation_summary_table_html.return }}
      filename: livestock_predation_summary_table.html
      filename_suffix: null

  - name: Create livestock predation summary table widget
    id: predation_summary_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Livestock Predation Summary"
      data: ${{ workflow.predation_summary_table_html_url.return }}

  - name: Format livestock species column 
    id: capitalize_livestock_species 
    task: format_text_column 
    partial: 
      df:  ${{ workflow.map_livestock_predation.return }}
      column: "Livestock Species"
      method: "capitalize"

  - name: Apply colormap to livestock predation events
    id: apply_livestock_colormap
    task: apply_color_map
    partial:
      input_column_name: "Livestock Species"
      output_column_name: colors
      colormap: tab10
      df: ${{ workflow.capitalize_livestock_species.return }}
  
  - name: Drop null geometry from livestock predation points 
    id: drop_null_livestock
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.apply_livestock_colormap.return }}
      geometry_column: "geometry"

  - name: Generate livestock predation point layers
    id: generate_livestock_layers
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      layer_style:
        get_fill_color: colors
        get_line_color: colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Livestock Species 
        label_column: "Livestock Species"
        color_column: colors
        sort: ascending
        label_suffix: null
      data_url: null
      geodataframe: ${{ workflow.drop_null_livestock.return }}

  - name: Combine styled layers with livestock predation events
    id: combine_custom_livestock
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_grazing_zones_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_livestock_layers.return }}

  - name: Draw livestock predation map
    id: draw_livestock_map
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_livestock.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}
        # longitude: 35.2092935880001
        # latitude: -1.257840580999955
        # zoom: 9.75
        # pitch: 0 
        # bearing: 0 

  - name: Persist livestock predation map HTML paths
    id: persist_livestock_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_livestock_map.return }}
      filename: livestock_predation_events.html
      filename_suffix: null

  - name: Create livestock predation map widget
    id: livestock_predation_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Livestock Predation Events Map"
      data: ${{ workflow.persist_livestock_urls.return }}

  - name: Convert livestock map to png
    id: convert_livestock_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_livestock_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Map livestock predation for summary table 
    id: map_livestock_summary 
    task: map_columns
    partial: 
      df: ${{ workflow.map_livestock_predation.return }}
      drop_columns: [] 
      retain_columns: [
        "date",
        "Livestock Species",
        "Suspected Predator",
        "Total livestock affected"
        ]
      rename_columns: {
        "Livestock Species":"livestock_species",
        "Suspected Predator":"suspected_predator",
        "Total livestock affected":"total_livestock_affected"
        }
      raise_if_not_found: false 

  - name: Replace nulls on livestock predator table with unknown
    id: replace_livestock_nulls
    task: fill_missing_values
    partial:
      df: ${{ workflow.map_livestock_summary.return }}
      subset: 
        - suspected_predator
        - livestock_species
      value: Unknown 
  
  - name: Map others with Unknown 
    id: map_livestock_unknown
    task: replace_column_values
    partial: 
      df: ${{ workflow.replace_livestock_nulls.return }}
      columns: 
        - suspected_predator
      value_map: 
        Other (specify in comments): Unknown
      inplace: true 
      errors: "ignore"

  - name: Convert livestock_affected col to int 
    id: convert_livestock_int
    task: ecoscope_workflows_ext_mnc.tasks.transformation.convert_columns_to_int
    partial: 
      df: ${{ workflow.map_livestock_unknown.return }}
      columns: 
        - total_livestock_affected
      errors: coerce
      fill_value: 0

  - name: Persist livestock summary table 
    id: persist_livestock_summary
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      filename: livestock_predation_summary_table
      df: ${{ workflow.convert_livestock_int.return }}

  - name: Map illegal grazing Events  
    id: map_illegal_grazing 
    task: map_columns
    partial:
      df: ${{ workflow.drop_illegal_prefix.return }}
      drop_columns: []
      retain_columns: [
        "date",
        "event_type",
        "geometry",
        "Herd Zone",
        "Landowner name",
        "action taken",
        ]
      rename_columns: {}
      raise_if_not_found: false 

# Draw illegal grazing map 
  - name: Drop null geometry from illegal grazing points
    id: drop_null_grazing
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.map_illegal_grazing.return }}
      geometry_column: "geometry"

  - name: Generate illegal grazing point layers
    id: generate_illegal_layers
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      layer_style:
        get_fill_color: [0, 0, 128]
        get_line_color: [0, 0, 128]
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Activity
        values: 
          - label: Illegal grazing
            color: "#000080"
        sort: null
        label_suffix: null
      data_url: null
      geodataframe: ${{ workflow.drop_null_grazing.return }}
  
  - name: Combine styled layers with illegal grazing layer
    id: combine_custom_illegal
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_grazing_zones_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_illegal_layers.return }}

  - name: Draw illegal grazing events map
    id: draw_illegal_grazing
    task: ecoscope_workflows_ext_custom.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_illegal.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist illegal grazing map HTML paths
    id: persist_illegal_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_illegal_grazing.return }}
      filename: illegal_grazing_map.html

  - name: Create illegal grazing map widget
    id: illegal_grazing_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Illegal Grazing Events Map"
      data: ${{ workflow.persist_illegal_urls.return }}

  - name: Convert illegal grazing map to png
    id: convert_illegal_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_illegal_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # 4. Wildlife Report 
  - name: Filter wildlife incident event types
    id: filter_wildlife_events
    task: filter_row_values
    partial:  
      df: ${{ workflow.events_temporal.return }}
      column: event_type
      values: 
        - snare_rep
        - fire_rep 
        - wildlife_injury_rep
        - wildlife_treatment_rep
        - wildlife_carcass_rep

  - name: Process wildlife incident event details 
    id: process_wild_incidents 
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_wildlife_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize wildlife events 
    id: normalize_wildlife_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_wild_incidents.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop wildlife incidents column prefix
    id: drop_wild_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_wildlife_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map wildlife incident event columns 
    id: map_wildlife_inc 
    task: map_columns 
    partial: 
      df: ${{ workflow.drop_wild_prefix.return }}
      drop_columns: [
        "index",
        "time",
        "event_category",
        "reported_by",
        "serial_number",
        ]
      retain_columns: []
      rename_columns: {}
      raise_if_not_found: false 
  
  - name: Persist wildlife incident events as csv 
    id: persist_wildlife_csv
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      filename: wildlife_events_recorded
      df: ${{ workflow.map_wildlife_inc.return }}

  - name: Generate wildlife summary table 
    id: generate_wild_summary 
    task: make_wildlife_summary_table
    partial: 
      df: ${{ workflow.map_wildlife_inc.return }}
      value_map: 
        fire_rep: Fire
        snare_rep: Snare 
        wildlife_carcass_rep: Wildlife carcass 
        wildlife_injury_rep: Injured wildlife
        wildlife_treatment_rep: Veterinary treatment
      max_unique: 6 
      shorten_width: 300 
      order: null 
  
  - name: Persist wildlife summary 
    id: persist_wildlife_df
    task: persist_df 
    partial:  
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filename: wildlife_incidents_summary_table 
      filetype: csv 
      df: ${{ workflow.generate_wild_summary.return }}

  - name: Calculate wildlife events recorded by date
    id: wildlife_events_recorded
    task: summarize_df
    partial:
      groupby_cols:
        - date
      summary_params:
        - display_name: no_of_events
          aggregator: nunique
          column: id
      reset_index: true
      df: ${{ workflow.map_wildlife_inc.return }}

  - name: Persist wildlife events by date
    id: wildlife_events_df
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.wildlife_events_recorded.return }}
      filename: wildlife_incidents_recorded_by_date

  # Draw Wildlife Predation Events
  - name: Drop null geometry from recorded wildlife incidents
    id: drop_null_inc
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.map_wildlife_inc.return }}
      geometry_column: "geometry"

  - name: Apply Colormap to wildlife predation events
    id: apply_wildlife_colormap
    task: apply_color_map
    partial:
      input_column_name: event_type
      output_column_name: colors
      colormap: tab10
      df: ${{ workflow.drop_null_inc.return }}
  
  - name: Map wildlife values 
    id: map_wildlife_values
    task: map_column_value
    partial: 
      df: ${{ workflow.apply_wildlife_colormap.return }}
      col: event_type
      new_col: event_type_mapped
      mapping: 
        fire_rep: Fire
        snare_rep: Snare 
        wildlife_carcass_rep: Wildlife carcass 
        wildlife_injury_rep: Injured wildlife
        wildlife_treatment_rep: Veterinary treatment
      default: Undefined 
      keep_unmapped: False

  - name: Generate wildlife predation point layers
    id: generate_wildlife_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: colors
        get_line_color: colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Wildlife Incidents
        label_column: event_type_mapped
        color_column: colors
        sort: ascending
      geodataframe: ${{ workflow.map_wildlife_values.return }}

  - name: Combine styled layers with wildlife incident events 
    id: combine_custom_wildlife
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_wildlife_layer.return }}

  - name: Draw wildlife incident map
    id: draw_wildlife_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_wildlife.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist wildlife predation map HTML paths
    id: persist_wildlife_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_wildlife_map.return }}
      filename: wildlife_incidents_map.html

  - name: Create wildlife incident map widget
    id: wildlife_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Wildlife Incident Map"
      data: ${{ workflow.persist_wildlife_urls.return }}

  - name: Convert wildlife incident map to png
    id: convert_wildlife_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_wildlife_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Wildlife sighting Events
  # Elephant Events
  - name: Retrieve elephant events
    id: retrieve_elephant_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: elephant_sighting_rep
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process elephant event details 
    id: process_elephant_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_elephant_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize elephant events 
    id: normalize_ele_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_elephant_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop elephant column prefix
    id: drop_ele_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_ele_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map elephant event columns
    id: map_elephant_cols
    task: map_columns
    partial:
      drop_columns: [
        "index",
        "time",
        "event_type",
        "event_category",
        "reported_by",
        "serial_number",
        "event_type_display",
        "Comments",
        ]
      retain_columns: []
      rename_columns: {
        "Female":"female",
        "Herd Demographic": "herd_composition",
        "Herd size":"herd_size",
        "Male":"male",
        "Subadult":"sub_adult",
        "< 1 year":"underayear",
        }
      df: ${{ workflow.drop_ele_prefix.return }}
      raise_if_not_found: false 

  - name: Fill missing values with Undefined for elephant events 
    id: fill_undefined_eles 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.map_elephant_cols.return }}
      value:  "Undefined"
      subset: ["herd_composition"]

  - name: Fill numerical missing values with 0 for elephant events 
    id: fill_num_eles 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.fill_undefined_eles.return }}
      value:  0
      subset:  ["underayear","female","male","herd_size","sub_adult"]

  - name: Convert elephant numeric values to int 
    id: convert_ele_num
    task: convert_columns_to_int 
    partial: 
      df: ${{ workflow.fill_num_eles.return }}
      errors: coerce 
      fill_value: 0
      columns: ["underayear","female","male","herd_size","sub_adult"]
  
  - name: Map elephant herd-composition values 
    id: map_ele_herd 
    task: map_column_value 
    partial: 
      df: ${{ workflow.convert_ele_num.return }}
      col: "herd_composition"
      mapping: {
        "bachelor":"Bachelor",
        "femalecalf":"Female + Calf",
        "mixed":"Mixed",
        "Undefined":"Unspecified",
        "Unspecified":"Unspecified",
        "Mixed":"Mixed",
        "Female + Calf": "Female + Calf",
        "Bachelor": "Bachelor"
        }
      new_col: "herd_composition_mapped"
      default: "Undefined"
      keep_unmapped: false

  - name: Apply classification to herd size (bins) for elephants
    id: apply_ele_bins
    task: apply_classification
    partial: 
      df: ${{ workflow.map_ele_herd.return }}
      input_column_name: "herd_size"
      output_column_name: "herd_size_bins"
      label_options: 
        label_ranges: true 
        label_decimals: 0 
      classification_options:
        scheme: "equal_interval"
        k: 5

  - name: Order binned herd size for elephants
    id: order_ele_bins 
    task: order_categorical_by_number 
    partial:  
      df: ${{ workflow.apply_ele_bins.return }}
      columns: ["herd_size_bins"]
      errors: ignore

  - name: Draw elephant bins bar chart 
    id: draw_ele_bins
    task: ecoscope_workflows_ext_wwf_virunga.tasks.plot.draw_bar_chart
    partial:
      dataframe: ${{ workflow.order_ele_bins.return }}
      category: herd_size_bins
      mode: grouped
      y_axis: null 
      stack_column: null 
      agg_function: count
      stack_order: null 
      ascending: true 
      color_column: null 
      plot_style: null 
      bar_chart_configs:
        - column: "id"
          agg_func: count
          label: "Number of records"
          show_label: false
          style:
            marker_color: "#6495ed"
            textposition": null
            texttemplate": null
      overlay_opacity: 0.6
      category_order: null 
      layout_style: 
        bargap: 0.05
        bargroupgap: 0.05 
        title: null 
        title_x: 0.01 
        title_y: 0.95 
        showlegend: false
        font_size: 11
        font_color: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Group size"
          range: null
        yaxis:
          title: "Number of records"
          range: null 
      widget_id: null

  - name: Persist elephant bar chart
    id: persist_elephant_bar
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_ele_bins.return }}
      filename: elephant_herd_size_bar_chart.html

  - name: Create elephant herd size bar chart widget
    id: ele_bar_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Elephant Herd Size Distribution"
      data: ${{ workflow.persist_elephant_bar.return }}

  - name: Convert elephant herd size bar chart to png
    id: convert_ele_bar_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_elephant_bar.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 10
        max_concurrent_pages: 1

  - name: Drop null geometry from elephant events
    id: drop_null_eles
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf:  ${{ workflow.order_ele_bins.return }} # summary table 
      geometry_column: "geometry"

  - name: Create elephant events clustered herd size layers
    id: eles_clustered_layer
    task: ecoscope_workflows_ext_big_life.tasks.results.create_clustered_labeled_scatterplot_layer
    partial: 
      data_url: null
      geodataframe: ${{ workflow.drop_null_eles.return }}
      layer_style: 
        get_value: "herd_size"
        aggregate: true
        cluster_padding: 1.0
        cluster_max_radius: 40
        get_fill_color: [100, 149, 237]
        get_line_color: [100, 149, 237]
        get_radius: "herd_size"
        get_line_width: 1
        radius_units:  "pixels"
        radius_scale: 1
        radius_min_pixels: 0
        radius_max_pixels: null
        line_width_units: "pixels"
        line_width_scale: 1
        line_width_min_pixels: 0
        line_width_max_pixels: null
        stroked: true
        filled: true
        text_style:
          get_text_color: [255, 255, 255]
          text_size: 15
          scale_text_to_radius: true
          size_units: "pixels"
          font_family: "monospace"
          font_weight: "normal"
          outline_width: null
          outline_color: null
          get_text_anchor: "middle"
          get_alignment_baseline:  "center"

      legend:
        title: Herd Size
        values:
          - label: Herds
            color: "#6495ed"

  - name: Combine styled layers with elephant herd size events 
    id: combine_ele_herd
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.eles_clustered_layer.return }}

  - name: Draw elephant bin map
    id: draw_ele_bin_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_ele_herd.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist elephant herd  map HTML paths
    id: persist_ele_herd_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_ele_bin_map.return }}
      filename: elephant_herd_types_map.html

  - name: Create elephant herd size map widget
    id: ele_herd_size_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Elephant Herd Size Map"
      data: ${{ workflow.persist_ele_herd_urls.return }}

  - name: Convert elephant herd size map to png
    id: convert_ele_herd_size_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_ele_herd_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Apply colormap for elephant herd types
    id: ele_herd_types 
    task: apply_color_map 
    partial:
      df: ${{ workflow.drop_null_eles.return }}
      input_column_name: herd_composition
      output_column_name: herd_composition_colors
      colormap: tab10

  - name: Generate elephant herd composition point layers
    id: ele_herd_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      layer_style:
        get_fill_color: herd_composition_colors
        get_line_color: herd_composition_colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Herd Types 
        label_column: "herd_composition"
        color_column: herd_composition_colors
        sort: ascending
        label_suffix: null
      data_url: null
      geodataframe: ${{ workflow.ele_herd_types.return }}

  - name: Combine styled layers with elephant herd size events 
    id: combine_ele_types
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.ele_herd_layer.return }}

  - name: Draw elephant bin map
    id: draw_ele_herd_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_ele_types.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist elephant events  map HTML paths
    id: persist_elephant_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_ele_herd_map.return }}
      filename: elephant_sightings_events.html

  - name: Create elephant herd composition map widget
    id: ele_herd_comp_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Elephant Herd Composition Map"
      data: ${{ workflow.persist_elephant_urls.return }}

  - name: Convert elephant herd composition map to png
    id: convert_ele_herd_comp_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_elephant_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  #   Buffalo
  - name: Retrieve buffalo events
    id: retrieve_buffalo_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: buffalo_sighting_rep
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process buffalo event details 
    id: process_buffalo_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_buffalo_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize buffalo events 
    id: normalize_buff_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_buffalo_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop buffalo column prefix
    id: drop_buff_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_buff_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map buffalo event columns
    id: map_buffalo_cols
    task: map_columns
    partial:
      drop_columns: [
        "index",
        "time",
        "event_type",
        "event_category",
        "reported_by",
        "serial_number",
        "event_type_display",
        "Comment",
        ]
      retain_columns: []
      rename_columns: {
        "Herd Demographic": "herd_composition",
        "Herd Size":"herd_size",
        }
      df: ${{ workflow.drop_buff_prefix.return }}
      raise_if_not_found: false 
  
  - name: Fill missing values with Undefined for buffalo events 
    id: fill_undefined_buff 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.map_buffalo_cols.return }}
      value:  "Undefined"
      subset: ["herd_composition"]

  - name: Fill numerical missing values with 0 for buffalo events 
    id: fill_num_buff 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.fill_undefined_buff.return }}
      value:  0
      subset:  ["herd_size"]

  - name: Convert buffalo numeric values to int 
    id: convert_buff_num
    task: convert_columns_to_int 
    partial: 
      df: ${{ workflow.fill_num_buff.return }}
      errors: coerce 
      fill_value: 0
      columns: ["herd_size"]
  
  - name: Map buffalo herd-composition values 
    id: map_buff_herd 
    task: map_column_value 
    partial: 
      df: ${{ workflow.convert_buff_num.return }}
      col: "herd_composition"
      mapping: {
        "bachelor":"Bachelor",
        "femalecalf":"Female + Calf",
        "mixed":"Mixed",
        "Undefined":"Unspecified",
        "Unspecified":"Unspecified",
        "Mixed":"Mixed",
        "Female + Calf": "Female + Calf",
        "Bachelor": "Bachelor"
        }
      new_col: "herd_composition_mapped"
      default: "Undefined"
      keep_unmapped: false

  - name: Apply classification to herd size (bins) for buffalos
    id: apply_buff_bins
    task: apply_classification
    partial: 
      df: ${{ workflow.map_buff_herd.return }}
      input_column_name: "herd_size"
      output_column_name: "herd_size_bins"
      label_options: 
        label_ranges: true 
        label_decimals: 0 
      classification_options:
        scheme: "equal_interval"
        k: 5

  - name: Order binned herd size for buffalos
    id: order_buff_bins 
    task: order_categorical_by_number 
    partial:  
      df: ${{ workflow.apply_buff_bins.return }}
      columns: ["herd_size_bins"]
      errors: ignore

  - name: Draw buffalo bins bar chart 
    id: draw_buff_bins
    task: ecoscope_workflows_ext_wwf_virunga.tasks.plot.draw_bar_chart
    partial:
      dataframe: ${{ workflow.order_buff_bins.return }}
      category: herd_size_bins
      mode: grouped
      y_axis: null 
      stack_column: null 
      agg_function: count
      stack_order: null 
      ascending: true 
      color_column: null 
      plot_style: null 
      bar_chart_configs:
        - column: "id"
          agg_func: count
          label: "Number of records"
          show_label: false
          style:
            marker_color: "#6495ed"
            textposition": null
            texttemplate": null
      overlay_opacity: 0.6
      category_order: null 
      layout_style: 
        bargap: 0.05
        bargroupgap: 0.05 
        title: null 
        title_x: 0.01 
        title_y: 0.95 
        showlegend: false
        font_size: 11
        font_color: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Group size"
          range: null
        yaxis:
          title: "Number of records"
          range: null 
      widget_id: null

  - name: Persist buffalo bar chart
    id: persist_buffalo_bar
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_buff_bins.return }}
      filename: buffalo_herd_size_bar_chart.html

  - name: Create buffalo herd size bar chart widget
    id: buff_bar_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Buffalo Herd Size Distribution"
      data: ${{ workflow.persist_buffalo_bar.return }}

  - name: Convert buffalo herd size bar chart to png
    id: convert_buff_bar_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_buffalo_bar.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 10
        max_concurrent_pages: 1

  - name: Drop null geometry from buffalo events
    id: drop_null_buff
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf:  ${{ workflow.order_buff_bins.return }} # summary table 
      geometry_column: "geometry"

  - name: Create buffalo events clustered herd size layers
    id: buff_clustered_layer
    task: ecoscope_workflows_ext_big_life.tasks.results.create_clustered_labeled_scatterplot_layer
    partial: 
      data_url: null
      geodataframe: ${{ workflow.drop_null_buff.return }}
      layer_style: 
        get_value: "herd_size"
        aggregate: true
        cluster_padding: 1.0
        cluster_max_radius: 40
        get_fill_color: [100, 149, 237]
        get_line_color: [100, 149, 237]
        get_radius: "herd_size"
        get_line_width: 1
        radius_units:  "pixels"
        radius_scale: 1
        radius_min_pixels: 0
        radius_max_pixels: null
        line_width_units: "pixels"
        line_width_scale: 1
        line_width_min_pixels: 0
        line_width_max_pixels: null
        stroked: true
        filled: true
        text_style:
          get_text_color: [255, 255, 255]
          text_size: 15
          scale_text_to_radius: true
          size_units: "pixels"
          font_family: "monospace"
          font_weight: "normal"
          outline_width: null
          outline_color: null
          get_text_anchor: "middle"
          get_alignment_baseline:  "center"

      legend:
        title: Herd Size
        values:
          - label: Herds
            color: "#6699ff"

  - name: Combine styled layers with buffalo herd size events 
    id: combine_buff_herd
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.buff_clustered_layer.return }}

  - name: Draw buffalo bin map
    id: draw_buff_bin_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_buff_herd.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist buffalo herd  map HTML paths
    id: persist_buff_herd_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_buff_bin_map.return }}
      filename: buffalo_herd_types_map.html

  - name: Create buffalo herd size map widget
    id: buff_herd_size_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Buffalo Herd Size Map"
      data: ${{ workflow.persist_buff_herd_urls.return }}

  - name: Convert buffalo herd size map to png
    id: convert_buff_herd_size_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_buff_herd_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Apply colormap for buffalo herd types
    id: buff_herd_types
    task: apply_color_map
    partial:
      df: ${{ workflow.drop_null_buff.return }}
      input_column_name: herd_composition
      output_column_name: herd_composition_colors
      colormap: tab10

  - name: Generate bufallo herd composition point layers
    id: buff_herd_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      layer_style:
        get_fill_color: herd_composition_colors
        get_line_color: herd_composition_colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Herd Types
        label_column: "herd_composition"
        color_column: herd_composition_colors
        sort: ascending
        label_suffix: null
      data_url: null
      geodataframe: ${{ workflow.buff_herd_types.return }}

  - name: Combine styled layers with buffalo herd size events 
    id: combine_buff_types
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.buff_herd_layer.return }}

  - name: Draw buffalo bin map
    id: draw_buff_herd_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_buff_types.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist buffalo events  map HTML paths
    id: persist_buffalo_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_buff_herd_map.return }}
      filename: buffalo_sightings_events.html

  - name: Create buffalo herd composition map widget
    id: buff_herd_comp_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Buffalo Herd Composition Map"
      data: ${{ workflow.persist_buffalo_urls.return }}

  - name: Convert buffalo herd composition map to png
    id: convert_buff_herd_comp_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_buffalo_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Lion
  - name: Retrieve lion events
    id: retrieve_lion_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: lion_sighting_rep
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process lion event details 
    id: process_lion_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_lion_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize lion events 
    id: normalize_lion_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_lion_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop lion column prefix
    id: drop_lion_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_lion_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map lion event columns
    id: map_lion_cols
    task: map_columns
    partial:
      drop_columns: [
        "index",
        "time",
        "event_type",
        "event_category",
        "reported_by",
        "serial_number",
        "event_type_display",
        "Comment",
        "Behavior",
        ]
      retain_columns: []
      rename_columns: {
        "Female":"female",
        "Male":"male",
        "Group Size":"group_size",
        "Individuals Present":"individuals_present",
        "Pride":"pride",
        "Young":"young",
        }
      df: ${{ workflow.drop_lion_prefix.return }}
      raise_if_not_found: false 

  - name: Fill missing values with Undefined for lion events
    id: fill_undefined_lion 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.map_lion_cols.return }}
      value:  "Undefined"
      subset: ["pride"]

  - name: Map lion pride values
    id: map_lion_pride
    task: map_column_value 
    partial: 
      df: ${{ workflow.fill_undefined_lion.return }}
      col: "pride"
      mapping: {
        "Undefined":"Undefined",
        "Other (specify in comments)":"Undefined",
        "Unknown":"Undefined",
        }
      new_col: "mapped_pride"
      default: "Undefined"
      keep_unmapped: true

  - name: Apply Colormap to lion pride events
    id: apply_lion_colormap # summary table
    task: apply_color_map
    partial:
      input_column_name: mapped_pride
      output_column_name: colors
      colormap: tab10
      df: ${{ workflow.map_lion_pride.return }} 

  - name: Drop null geometry from recorded lion incidents
    id: drop_null_lion
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.apply_lion_colormap.return }}
      geometry_column: "geometry"

  - name: Generate lion pride point layers
    id: generate_lion_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: colors
        get_line_color: colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: pride
        label_column: mapped_pride
        color_column: colors
        sort: ascending
      geodataframe: ${{ workflow.drop_null_lion.return }}

  - name: Combine styled layers with lion sighting events 
    id: combine_custom_lion
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_lion_layer.return }}

  - name: Draw lion sighting events map
    id: draw_lion_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_lion.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist lion sighting map HTML paths
    id: persist_lion_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_lion_map.return }}
      filename: lion_pride_sightings_map.html

  - name: Create lion sightings map widget
    id: lion_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Lion Sightings Map"
      data: ${{ workflow.persist_lion_urls.return }}

  - name: Convert lion sightings map to png
    id: convert_lion_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_lion_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Leopard sighting
  - name: Retrieve leopard events
    id: retrieve_leo_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: leopardsightingrep
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process leopard event details 
    id: process_leo_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_leo_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize leopard events 
    id: normalize_leo_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_leo_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop leopard column prefix
    id: drop_leo_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_leo_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map leopard event columns
    id: map_leopard_cols
    task: map_columns
    partial:
      drop_columns: [
        "index",
        "time",
        "event_type",
        "event_category",
        "reported_by",
        "serial_number",
        "event_type_display",
        "Comment",
        "Behavior",
        ]
      retain_columns: []
      rename_columns: {
        "Female":"female",
        "Male":"male",
        "Group Size":"group_size",
        "Individuals Present":"individuals_present",
        "Young":"young",
        }
      df: ${{ workflow.drop_leo_prefix.return }}
      raise_if_not_found: false 

  - name: Fill missing values with Undefined for leopard events
    id: fill_undefined_leo 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.map_leopard_cols.return }}
      value:  "Undefined"
      subset: ["individuals_present"]

  - name: Map leopard values
    id: map_leopards
    task: map_column_value 
    partial: 
      df: ${{ workflow.fill_undefined_leo.return }}
      col: "individuals_present"
      mapping: {
        "Undefined":"Undefined",
        "Other (specify in comments)":"Undefined",
        "Unknown":"Undefined",
        }
      new_col: "individuals_present_mapped"
      default: "Undefined"
      keep_unmapped: true

  - name: Apply Colormap to leopard  events
    id: apply_leo_colormap # summary table
    task: apply_color_map
    partial:
      input_column_name: "individuals_present_mapped"
      output_column_name: colors
      colormap: tab10
      df: ${{ workflow.map_leopards.return }} 

  - name: Drop null geometry from recorded leopard incidents
    id: drop_null_leo
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.apply_leo_colormap.return }}
      geometry_column: "geometry"

  - name: Generate leopard point layers
    id: generate_leo_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: colors
        get_line_color: colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Individuals
        label_column: "individuals_present_mapped"
        color_column: colors
        sort: ascending
      geodataframe: ${{ workflow.drop_null_leo.return }}

  - name: Combine styled layers with leopard sighting events 
    id: combine_custom_leo
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_leo_layer.return }}

  - name: Draw leopard sighting events map
    id: draw_leo_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_leo.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist leopard sighting map HTML paths
    id: persist_leo_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_leo_map.return }}
      filename: leopard_sightings_map.html

  - name: Create leopard sightings map widget
    id: leo_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Leopard Sightings Map"
      data: ${{ workflow.persist_leo_urls.return }}

  - name: Convert leopard sightings map to png
    id: convert_leo_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_leo_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Cheetah
  - name: Retrieve cheetah events
    id: retrieve_chee_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: cheetah_sighting_rep
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process cheetah event details 
    id: process_chee_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_chee_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize cheetah events 
    id: normalize_chee_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_chee_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop cheetah column prefix
    id: drop_chee_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_chee_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Map cheetah event columns
    id: map_chee_cols
    task: map_columns
    partial:
      drop_columns: [
        "index",
        "time",
        "event_type",
        "event_category",
        "reported_by",
        "serial_number",
        "event_type_display",
        "Comment",
        "Behavior",
        ]
      retain_columns: []
      rename_columns: {
        "Female":"female",
        "Male":"male",
        "Group Size":"group_size",
        "Individuals Present":"individuals_present",
        "Young":"young",
        }
      df: ${{ workflow.drop_chee_prefix.return }}
      raise_if_not_found: false 

  - name: Fill missing values with Undefined for cheetah events
    id: fill_undefined_chee
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.map_chee_cols.return }}
      value:  "Undefined"
      subset: ["individuals_present"]

  - name: Map cheetah values
    id: map_cheetah
    task: map_column_value 
    partial: 
      df: ${{ workflow.fill_undefined_chee.return }}
      col: "individuals_present"
      mapping: {
        "Undefined":"Undefined",
        "Other (specify in comments)":"Undefined",
        "Unknown":"Undefined",
        }
      new_col: "individuals_present_mapped"
      default: "Undefined"
      keep_unmapped: true

  - name: Apply Colormap to cheetah events
    id: apply_chee_colormap # summary table
    task: apply_color_map
    partial:
      input_column_name: "individuals_present_mapped"
      output_column_name: colors
      colormap: tab10
      df: ${{ workflow.map_cheetah.return }} 

  - name: Drop null geometry from recorded cheetah incidents
    id: drop_null_chee
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.apply_chee_colormap.return }}
      geometry_column: "geometry"

  - name: Generate cheetah point layers
    id: generate_chee_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: colors
        get_line_color: colors
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Individuals
        label_column: "individuals_present_mapped"
        color_column: colors
        sort: ascending
      geodataframe: ${{ workflow.drop_null_chee.return }}

  - name: Combine styled layers with cheetah sighting events 
    id: combine_custom_chee
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_chee_layer.return }}

  - name: Draw chee sighting events map
    id: draw_chee_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_chee.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist cheetah sighting map HTML paths
    id: persist_chee_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_chee_map.return }}
      filename: cheetah_sightings_map.html

  - name: Create cheetah sightings map widget
    id: chee_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Cheetah Sightings Map"
      data: ${{ workflow.persist_chee_urls.return }}

  - name: Convert cheetah sightings map to png
    id: convert_chee_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_chee_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Giraffe
  - name: Retrieve giraffe events
    id: retrieve_gir_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: giraffe_sighting
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process giraffe event details 
    id: process_gir_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_gir_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize giraffe events 
    id: normalize_gir_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_gir_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop giraffe column prefix
    id: drop_gir_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_gir_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Drop null geometry from recorded giraffe incidents
    id: drop_null_gir
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.drop_gir_prefix.return }}
      geometry_column: "geometry"

  - name: Generate giraffe point layers
    id: generate_gir_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: [72, 61, 139]
        get_line_color: [72, 61, 139]
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Giraffe Sightings
        values:
          - label: Sighting
            color: "#483d8b"
      geodataframe: ${{ workflow.drop_null_gir.return }}

  - name: Combine styled layers with giraffe sighting events 
    id: combine_custom_gir
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_gir_layer.return }}

  - name: Draw giraffe sighting events map
    id: draw_gir_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_gir.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist giraffe sighting map HTML paths
    id: persist_gir_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_gir_map.return }}
      filename: giraffe_sightings_map.html

  - name: Create giraffe sightings map widget
    id: gir_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Giraffe Sightings Map"
      data: ${{ workflow.persist_gir_urls.return }}

  - name: Convert giraffe sightings map to png
    id: convert_gir_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_gir_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Rhino
  - name: Retrieve rhino events
    id: retrieve_rhi_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: rhino_sighting_rep
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process rhino event details 
    id: process_rhi_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_rhi_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize rhino events 
    id: normalize_rhi_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_rhi_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop rhino column prefix
    id: drop_rhi_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_rhi_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Drop null geometry from recorded rhino incidents
    id: drop_null_rhi
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.drop_rhi_prefix.return }}
      geometry_column: "geometry"

  - name: Generate rhino point layers
    id: generate_rhi_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: [72, 61, 139]
        get_line_color: [72, 61, 139]
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Rhino Sightings
        values:
          - label: Sighting
            color: "#483d8b"
      geodataframe: ${{ workflow.drop_null_rhi.return }}

  - name: Combine styled layers with rhino sighting events 
    id: combine_custom_rhi
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_rhi_layer.return }}

  - name: Draw rhino sighting events map
    id: draw_rhi_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_rhi.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist rhino sighting map HTML paths
    id: persist_rhi_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_rhi_map.return }}
      filename: rhino_sightings_map.html

  - name: Create rhino sightings map widget
    id: rhi_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Rhino Sightings Map"
      data: ${{ workflow.persist_rhi_urls.return }}

  - name: Convert rhino sightings map to png
    id: convert_rhi_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_rhi_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Hartebeest
  - name: Retrieve hartebeest events
    id: retrieve_hart_events 
    task: filter_df 
    partial: 
      column_name: event_type
      op: equal 
      value: hartebeest_sighting
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process hartebeest event details 
    id: process_hart_evs 
    task: process_events_details 
    partial: 
      df: ${{ workflow.retrieve_hart_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize hartebeest events 
    id: normalize_hart_events 
    task: normalize_json_column
    partial:  
      column: event_details 
      df: ${{ workflow.process_hart_evs.return }}
      skip_if_not_exists: true 
      sort_columns: true

  - name: Drop hartebeest column prefix
    id: drop_hart_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_hart_events.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Drop null geometry from recorded hartebeest incidents
    id: drop_null_hart
    task: ecoscope_workflows_ext_custom.tasks.transformation.drop_null_geometry
    skipif:
      conditions:
        - any_is_empty_df
        - any_dependency_skipped
    partial:
      gdf: ${{ workflow.drop_hart_prefix.return }}
      geometry_column: "geometry"

  - name: Generate hartebeest point layers
    id: generate_hart_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_scatterplot_layer
    partial:
      data_url: null
      layer_style:
        get_fill_color: [72, 61, 139]
        get_line_color: [72, 61, 139]
        get_radius: 3
        opacity: 0.55
        stroked: true
      legend:
        title: Hartebeest Sightings
        values:
          - label: Sighting
            color: "#483d8b"
      geodataframe: ${{ workflow.drop_null_hart.return }}

  - name: Combine styled layers with hartebeest sighting events 
    id: combine_custom_hart
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_hart_layer.return }}

  - name: Draw hartebeest sighting events map
    id: draw_hart_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_custom_hart.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist hartebeest sighting map HTML paths
    id: persist_hart_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_hart_map.return }}
      filename: hartebeest_sightings_map.html

  - name: Create hartebeest sightings map widget
    id: hart_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Hartebeest Sightings Map"
      data: ${{ workflow.persist_hart_urls.return }}

  - name: Convert hartebeest sightings map to png
    id: convert_hart_map_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_hart_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  # Summary tables
  - name: overall elephant summary table 
    id: overall_ele_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.order_ele_bins.return }}
      groupby_cols:
        - "herd_composition_mapped"
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true

  - name: Rename elephant summary table columns
    id: rename_ele_table
    task: map_columns
    partial:
      df: ${{ workflow.overall_ele_table.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "herd_composition_mapped": "Herd Type",
        }
      raise_if_not_found: false

  - name: Persist overall elephant summary table
    id: persist_ele_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_ele_table.return }}
      filename: overall_elephant_summary_table

  - name: Draw overall elephant summary table
    id: ele_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_ele_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall elephant summary table (HTML)
    id: ele_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.ele_table_html.return }}
      filename: overall_elephant_summary_table.html

  - name: Create elephant summary table widget
    id: ele_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Elephant Herd Composition Summary"
      data: ${{ workflow.ele_table_html_url.return }}

  # Buffalo
  - name: overall buffalo summary table 
    id: overall_buff_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.order_buff_bins.return }}
      groupby_cols:
        - "herd_composition_mapped"
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true

  - name: Rename buffalo summary table columns
    id: rename_buff_table
    task: map_columns
    partial:
      df: ${{ workflow.overall_buff_table.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "herd_composition_mapped": "Herd Type",
        }
      raise_if_not_found: false

  - name: Persist overall elephant summary table
    id: persist_buff_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_buff_table.return }}
      filename: overall_buffalo_summary_table

  - name: Draw overall buffalo summary table
    id: buff_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_buff_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall buffalo summary table (HTML)
    id: buff_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.buff_table_html.return }}
      filename: overall_buffalo_summary_table.html

  - name: Create buffalo summary table widget
    id: buff_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Buffalo Herd Composition Summary"
      data: ${{ workflow.buff_table_html_url.return }}

  # Cheetah
  - name: overall cheetah summary table
    id: overall_chee_table
    task: summarize_df
    partial:
      df: ${{ workflow.apply_chee_colormap.return }}
      groupby_cols:
        - "individuals_present_mapped"
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true

  - name: Rename cheetah summary table columns
    id: rename_chee_table
    task: map_columns
    partial:
      df: ${{ workflow.overall_chee_table.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "individuals_present_mapped": "Individuals",
        }
      raise_if_not_found: false

  - name: Persist overall cheetah summary table
    id: persist_chee_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_chee_table.return }}
      filename: overall_cheetah_summary_table

  - name: Draw overall cheetah summary table
    id: chee_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_chee_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall cheetah summary table (HTML)
    id: chee_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.chee_table_html.return }}
      filename: overall_cheetah_summary_table.html

  - name: Create cheetah summary table widget
    id: chee_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Cheetah Sightings Summary"
      data: ${{ workflow.chee_table_html_url.return }}

  # Lion
  - name: overall lion summary table 
    id: overall_lion_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.apply_lion_colormap.return }}
      groupby_cols:
        - "mapped_pride"
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true

  - name: Rename lion summary table columns
    id: rename_lion_table
    task: map_columns
    partial:
      df: ${{ workflow.overall_lion_table.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "mapped_pride": "Pride",
        }
      raise_if_not_found: false

  - name: Persist overall lion summary table
    id: persist_lion_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_lion_table.return }}
      filename: overall_lion_summary_table

  - name: Draw overall lion summary table
    id: lion_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_lion_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall lion summary table (HTML)
    id: lion_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.lion_table_html.return }}
      filename: overall_lion_summary_table.html

  - name: Create lion summary table widget
    id: lion_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Lion Sightings Summary"
      data: ${{ workflow.lion_table_html_url.return }}

  # Leopard
  - name: overall leopard summary table 
    id: overall_leo_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.apply_leo_colormap.return }}
      groupby_cols:
        - "individuals_present_mapped"
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true

  - name: Rename leopard summary table columns
    id: rename_leo_table
    task: map_columns
    partial:
      df: ${{ workflow.overall_leo_table.return }}
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "individuals_present_mapped": "Individuals",
        }
      raise_if_not_found: false

  - name: Persist overall leopard summary table
    id: persist_leo_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.rename_leo_table.return }}
      filename: overall_leopard_summary_table

  - name: Draw overall leopard summary table
    id: leo_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.rename_leo_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall leopard summary table (HTML)
    id: leo_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.leo_table_html.return }}
      filename: overall_leopard_summary_table.html

  - name: Create leopard summary table widget
    id: leo_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Leopard Sightings Summary"
      data: ${{ workflow.leo_table_html_url.return }}

  # Giraffe
  - name: overall giraffe summary table 
    id: overall_gir_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.drop_gir_prefix.return }}
      groupby_cols:
        - date
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true 
  
  - name: Persist overall giraffe summary table
    id: persist_gir_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.overall_gir_table.return }}
      filename: overall_giraffe_summary_table

  - name: Draw overall giraffe summary table
    id: gir_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.overall_gir_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall giraffe summary table (HTML)
    id: gir_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.gir_table_html.return }}
      filename: overall_giraffe_summary_table.html

  - name: Create giraffe summary table widget
    id: gir_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Giraffe Sightings Summary"
      data: ${{ workflow.gir_table_html_url.return }}

  # Rhino
  - name: overall rhino summary table 
    id: overall_rhi_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.drop_rhi_prefix.return }}
      groupby_cols:
        - date
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true 
  
  - name: Persist overall rhino summary table
    id: persist_rhi_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.overall_rhi_table.return }}
      filename: overall_rhino_summary_table

  - name: Draw overall rhino summary table
    id: rhi_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.overall_rhi_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall rhino summary table (HTML)
    id: rhi_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.rhi_table_html.return }}
      filename: overall_rhino_summary_table.html

  - name: Create rhino summary table widget
    id: rhi_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Rhino Sightings Summary"
      data: ${{ workflow.rhi_table_html_url.return }}

  # Hartebeest
  - name: overall hartebeest summary table 
    id: overall_hart_table 
    task: summarize_df 
    partial: 
      df: ${{ workflow.drop_hart_prefix.return }}
      groupby_cols:
        - date
      summary_params: 
        - display_name: observations
          aggregator: nunique
          column: id 
          decimal_places: 0 
      reset_index: true 
  
  - name: Persist overall hartebeest summary table
    id: persist_hart_summary
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.overall_hart_table.return }}
      filename: overall_hart_summary_table

  - name: Draw overall hartebeest summary table
    id: hart_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.overall_hart_table.return }}
      columns: null
      table_config:
        enable_sorting: true
        enable_filtering: true
        enable_download: false
        hide_header: false

  - name: Persist overall hartebeest summary table (HTML)
    id: hart_table_html_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.hart_table_html.return }}
      filename: overall_hart_summary_table.html

  - name: Create hartebeest summary table widget
    id: hart_table_widget
    task: create_table_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: "Hartebeest Sightings Summary"
      data: ${{ workflow.hart_table_html_url.return }}

  # 5. Patrol Report 
  - name: Exclude specific events types from total events
    id: filter_events
    task: exclude_row_values
    partial:  
      df: ${{ workflow.events_temporal.return }}
      column: event_type 
      values:
        - distancecountwildlife_rep
        - distancecountpatrol_rep
        - airstrip_operations

  - name: Calculate total events
    id: total_events_recorded
    task: summarize_df
    partial:
      groupby_cols:
        - date
      summary_params:
        - display_name: no_of_events
          aggregator: nunique
          column: id
      reset_index: true
      df: ${{ workflow.filter_events.return }}
  
  - name: Persist total events df 
    id: persist_tevents_df
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.total_events_recorded.return }}
      filename: total_events_recorded_by_date

  - name: Calculate total events recorded by event type
    id: total_events_type_recorded
    task: summarize_df
    partial:
      groupby_cols:
        - date
        - event_type
      summary_params:
        - display_name: no_of_events
          aggregator: nunique
          column: id
      reset_index: true
      df: ${{ workflow.filter_events.return }}

  - name: Persist total events by event type df
    id: persist_summary_event_type
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.total_events_type_recorded.return }}
      filename: total_events_recorded_by_type

  - name: Draw total events line chart 
    id: draw_events_chart 
    task: ecoscope.platform.tasks.results.draw_line_chart
    partial:
      smoothing: null
      dataframe: ${{ workflow.total_events_recorded.return }}
      x_column: date 
      y_column: no_of_events
      category_column: null
      line_kwargs:
        shape: "linear"
        color: "lightsteelblue"
      layout_kwargs:
        title: null
        title_x: 0.01
        title_y:  0.95
        showlegend: false
        fontsize: 10
        fontcolor: "#222222"
        plot_bgcolor: "#f5f5f5"
        xaxis:
          title: "Date"
          tickformat: "%Y-%m-%d"
          tickangle: -45
          rangemode: auto
        yaxis:
          title: "Number of events"
          tickformat: ".1f"
        hovermode: "x unified"

  - name: Persist Total events chart as text
    id: persist_total_events
    task: persist_text
    partial:
      root_path:  ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_events_chart.return }}
      filename: total_events_recorded.html # -- convert this to .png

  - name: Convert total events chart to png
    id: convert_events_chart_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_total_events.return }}
      config:
        width: 1280
        height: 720
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 10
        timeout: 0
        max_concurrent_pages: 5

  - name: Create total events chart widget
    id: events_chart_widget
    task: create_plot_widget_single_view
    partial:
      title: "Total Events Recorded"
      data: ${{ workflow.persist_total_events.return }}

  # Patrol Information
  - name : Filter events and get patrol information 
    id: filter_patrol_info_events
    task: filter_df
    partial:
      column_name: event_type
      op: equal
      value: patrol_info
      df: ${{ workflow.events_temporal.return }}
      reset_index: false

  - name: Process patrol event details 
    id: process_patrol_events
    task: process_events_details 
    partial: 
      df: ${{ workflow.filter_patrol_info_events.return }}
      client: ${{ workflow.er_client_name.return }}
      map_to_titles: true 
      ordered: true

  - name: Normalize patrol events
    id: normalize_patrols
    task: normalize_json_column
    partial:
      column: event_details
      df: ${{ workflow.process_patrol_events.return }}
      skip_if_not_exists: true 
      sort_columns: true 

  - name: Drop patrol column prefix
    id: drop_patrol_prefix 
    task: drop_column_prefix 
    partial: 
      df: ${{ workflow.normalize_patrols.return }}
      prefix: event_details__
      duplicate_strategy: "keep_original"

  - name: Persist events 
    id: persist_events 
    task: persist_df 
    partial: 
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      filename: patrol_events
      df: ${{ workflow.drop_patrol_prefix.return }}

  - name: Rename patrol information columns 
    id: rename_patrol_info
    task: map_columns
    partial:
      raise_if_not_found: true
      drop_columns: [
        # "time",
        # "event_type",
        # "event_category",
        # "reported_by",
        # "serial_number",
      ]
      retain_columns: []
      rename_columns: {
        "patrols":"patrol_id",
        "Participants":"participants",
        "Patrol Purpose":"patrol_purpose",
        "Transport Type":"transport_type"
      }

      df: ${{ workflow.drop_patrol_prefix.return }}

  - name: Patrol information Summary
    id: patrol_info_summary
    task: summarize_df
    partial:
      groupby_cols:
        - patrol_purpose
      summary_params:
        - display_name: number_of_patrols
          aggregator: nunique 
          column: id 
          original_unit: null 
          new_unit: null 
          decimal_places: 0
      reset_index: true
      df: ${{ workflow.rename_patrol_info.return }}

  - name: Persist patrol summary table 
    id: persist_patrol_df
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      filename: patrol_purpose_summary
      df: ${{ workflow.patrol_info_summary.return }}

  - name: Draw patrol purpose summary table
    id: patrol_summary_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.patrol_info_summary.return }}
      columns: null
      table_config: {enable_sorting: true, enable_filtering: true, enable_download: false, hide_header: false}
      widget_id: "Patrol Purpose Summary"

  - name: Save patrol purpose summary table (HTML)
    id: patrol_summary_table_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.patrol_summary_table_html.return }}
      filename: patrol_purpose_summary_table.html

  - name: Create patrol purpose summary table widget
    id: patrol_summary_table_widget
    task: create_table_widget_single_view
    partial:
      title: "Patrol Purpose Summary"
      data: ${{ workflow.patrol_summary_table_url.return }}

  # patrol observations
  - name: Filter null values from patrol id 
    id: filter_null_patrols
    task: filter_notna 
    partial: 
      df: ${{ workflow.rename_patrol_info.return }}
      column_names: ["patrol_id"]

  - name: Fill missing values in transport_type column 
    id: fill_transport_type 
    task: fill_missing_values 
    partial: 
      df: ${{ workflow.filter_null_patrols.return }}
      value: "Undefined"
      subset: ["transport_type"]

  - name: Explode patrol id values  
    id: explode_patrol_id 
    task: explode 
    partial: 
      df: ${{ workflow.fill_transport_type.return }}
      column_name: "patrol_id"
      ignore_index: true 

  - name: Get patrol values from events 
    id: get_patrol_event_values 
    task: get_patrol_values 
    partial: 
      events_df: ${{ workflow.explode_patrol_id.return }}
      patrols_column: patrol_id
      client: ${{ workflow.er_client_name.return }}
      max_workers: 10
  
  - name: Get patrol observations 
    id: get_patrol_obs 
    task: get_patrol_observations_from_patrols_df 
    partial: 
      client: ${{ workflow.er_client_name.return }}
      patrols_df: ${{ workflow.get_patrol_event_values.return }}
      include_patrol_details: true 
      raise_on_empty: true 
      sub_page_size: 100 

  - name: Join patrols df 
    id: join_patrol_df 
    task: map_columns
    partial: 
      df: ${{ workflow.explode_patrol_id.return }}
      drop_columns: []
      retain_columns: [
        "id",
        "patrol_id",
        "participants",
        "patrol_purpose",
        "transport_type"
        ]
      rename_columns: {}
      raise_if_not_found: false

  - name: Merge patrol events and patrol observations
    id: merge_patrol_df_obs 
    task: merge_two_dataframes 
    partial: 
      left: ${{ workflow.get_patrol_obs.return }}
      right: ${{ workflow.join_patrol_df.return }}
      on: "patrol_id"
      left_on: null
      right_on: null
      left_index: false
      right_index: false
      fillna_value:  null
      how: inner

  - name: Explode participants columns on observation   
    id: explode_participants 
    task: explode 
    partial: 
      df: ${{ workflow.merge_patrol_df_obs.return }}
      ignore_index: true 
      column_name: "participants"

  - name: Transform observations to relocations
    id: obs_relocs
    task: process_relocations
    partial:
      observations: ${{ workflow.explode_participants.return }}
      relocs_columns: [
        "extra__id",
        "extra__created_at",
        "extra__subject_id",
        "geometry",
        "groupby_col",
        "fixtime",
        "junk_status",
        "patrol_id",
        "patrol_title",
        "patrol_serial_number",
        "patrol_start_time",
        "patrol_end_time",
        "patrol_type",
        "patrol_status",
        "patrol_subject",
        "patrol_type__value",
        "participants",
        "patrol_purpose",
        "transport_type"
        ]
      filter_point_coords:
        - x: 180.000000
          y: 90.000000
        - x: 0.000000
          y: 0.000000
        - x: 1.000000
          y: 1.000000

  - name: Persist patrol relocations as geoparquet
    id: persist_relocs
    task: persist_df 
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: geoparquet 
      filename: patrol_relocations
      df: ${{ workflow.obs_relocs.return }}


  - name: Filter foot patrol observations 
    id: filter_foot_patrols
    task: filter_df
    partial:
      column_name: transport_type
      op: equal
      value: Foot
      df:  ${{ workflow.obs_relocs.return }}
      reset_index: true

  - name: Filter vehicle patrol observations 
    id: filter_vehicle_patrols
    task: filter_df
    partial:
      column_name: transport_type
      op: equal
      value: Vehicle
      df:  ${{ workflow.obs_relocs.return }}
      reset_index: false

  - name: Filter motorbike patrol observations 
    id: filter_motor_patrols
    task: filter_df
    partial:
      column_name: transport_type
      op: equal
      value: Motorbike
      df:  ${{ workflow.obs_relocs.return }}
      reset_index: false


  - name: Convert vehicle relocations to trajectories
    id: vehicle_trajs
    task: relocations_to_trajectory
    partial:
      relocations: ${{ workflow.filter_vehicle_patrols.return }}
      trajectory_segment_filter:
        min_length_meters: 0.35
        max_length_meters: 5000.0
        max_time_secs: 18000.0
        min_time_secs: 1.0
        max_speed_kmhr: 100.0
        min_speed_kmhr: 10.0
      
  - name: Convert motorbike relocations to trajectories
    id: motor_trajs
    task: relocations_to_trajectory
    partial:
      relocations: ${{ workflow.filter_motor_patrols.return }}
      trajectory_segment_filter:
        min_length_meters: 0.35
        max_length_meters: 5000.0
        max_time_secs: 18000.0
        min_time_secs: 1.0
        max_speed_kmhr: 100.0
        min_speed_kmhr: 10.0

  - name: Convert foot relocations to trajectories
    id: foot_trajs
    task: relocations_to_trajectory
    partial:
      relocations: ${{ workflow.filter_foot_patrols.return }}
      trajectory_segment_filter:
        min_length_meters: 0.001
        max_length_meters: 5000.0
        max_time_secs: 14400.0
        min_time_secs: 1.0
        max_speed_kmhr: 9.0
        min_speed_kmhr: 0.5

  - name: Add temporal index to foot trajs
    id: temporal_foot_traj
    task: add_temporal_index
    partial:
      df: ${{ workflow.foot_trajs.return }}
      time_col: segment_start
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true
      format: mixed

  - name: Add temporal index to vehicle trajs
    id: temporal_vehicle_traj
    task: add_temporal_index
    partial:
      df: ${{ workflow.vehicle_trajs.return }}
      time_col: segment_start
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true
      format: mixed

  - name: Add temporal index to motorbike trajs
    id: temporal_motor_traj
    task: add_temporal_index
    partial:
      df: ${{ workflow.motor_trajs.return }}
      time_col: segment_start
      groupers: ${{ workflow.groupers.return }}
      cast_to_datetime: true
      format: mixed

  - name: Rename foot traj columns
    id: rename_foot_trajs
    task: map_columns
    partial:
      raise_if_not_found: true
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "extra__created_at":"created_at",
        "extra__id":"id",
        "extra__participants":"participants",
        "extra__patrol_end_time":"patrol_end_time",
        "extra__patrol_id":"patrol_id",
        "extra__patrol_purpose":"patrol_purpose",
        "extra__patrol_serial_number":"patrol_serial_number",
        "extra__patrol_start_time":"patrol_start_time",
        "extra__patrol_status":"patrol_status",
        "extra__patrol_subject":"patrol_subject",
        "extra__patrol_title":"patrol_title",
        "extra__patrol_type":"patrol_type",
        "extra__patrol_type__value":"patrol_type_value",
        "extra__subject_id":"subject_id",
        "extra__transport_type":"transport_type"
        }
      df: ${{ workflow.temporal_foot_traj.return }}

  - name: Rename vehicle traj columns
    id: rename_vehicle_trajs
    task: map_columns
    partial:
      raise_if_not_found: true
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "extra__created_at":"created_at",
        "extra__id":"id",
        "extra__participants":"participants",
        "extra__patrol_end_time":"patrol_end_time",
        "extra__patrol_id":"patrol_id",
        "extra__patrol_purpose":"patrol_purpose",
        "extra__patrol_serial_number":"patrol_serial_number",
        "extra__patrol_start_time":"patrol_start_time",
        "extra__patrol_status":"patrol_status",
        "extra__patrol_subject":"patrol_subject",
        "extra__patrol_title":"patrol_title",
        "extra__patrol_type":"patrol_type",
        "extra__patrol_type__value":"patrol_type_value",
        "extra__subject_id":"subject_id",
        "extra__transport_type":"transport_type"
        }
      df: ${{ workflow.temporal_vehicle_traj.return }}

  - name: Rename motor traj columns
    id: rename_motor_trajs
    task: map_columns
    partial:
      raise_if_not_found: true
      drop_columns: []
      retain_columns: []
      rename_columns: {
        "extra__created_at":"created_at",
        "extra__id":"id",
        "extra__participants":"participants",
        "extra__patrol_end_time":"patrol_end_time",
        "extra__patrol_id":"patrol_id",
        "extra__patrol_purpose":"patrol_purpose",
        "extra__patrol_serial_number":"patrol_serial_number",
        "extra__patrol_start_time":"patrol_start_time",
        "extra__patrol_status":"patrol_status",
        "extra__patrol_subject":"patrol_subject",
        "extra__patrol_title":"patrol_title",
        "extra__patrol_type":"patrol_type",
        "extra__patrol_type__value":"patrol_type_value",
        "extra__subject_id":"subject_id",
        "extra__transport_type":"transport_type"
        }
      df: ${{ workflow.temporal_motor_traj.return }}

  - name: Summarize foot patrol metrics
    id: foot_patrol_metrics
    task: summarize_df
    partial:
      groupby_cols:
        - patrol_type_value
      summary_params:
        - display_name: no_of_patrols
          aggregator: nunique
          column: patrol_id

        - display_name: distance_km
          aggregator: sum
          column: dist_meters
          original_unit: m
          new_unit: km

        - display_name: duration_hrs
          aggregator: sum
          column: timespan_seconds
          original_unit: s
          new_unit: h

        - display_name: average_speed 
          aggregator: mean 
          column: speed_kmhr
      reset_index: true
      df: ${{ workflow.rename_foot_trajs.return }}

  - name: Persist foot patrol metrics
    id: persist_foot_df
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      filename: foot_patrol_efforts
      df: ${{ workflow.foot_patrol_metrics.return }}

  - name: Summarize vehicle patrol metrics
    id: vehicle_patrol_metrics
    task: summarize_df
    partial:
      groupby_cols:
        - patrol_type_value
      summary_params:
        - display_name: no_of_patrols
          aggregator: nunique
          column: patrol_id
        - display_name: distance_km
          aggregator: sum
          column: dist_meters
          original_unit: m
          new_unit: km
        - display_name: duration_hrs
          aggregator: sum
          column: timespan_seconds
          original_unit: s
          new_unit: h
        - display_name: average_speed 
          aggregator: mean 
          column: speed_kmhr
      reset_index: true
      df: ${{ workflow.rename_vehicle_trajs.return }}

  - name: Persist vehicle patrol metrics
    id: persist_vehicle_df
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.vehicle_patrol_metrics.return }}
      filename: vehicle_patrol_efforts

 # Motorbike patrol metrics and persist
  - name: Summarize motorbike patrol metrics
    id: motor_patrol_metrics
    task: summarize_df
    partial:
      groupby_cols:
        - patrol_type_value
      summary_params:
        - display_name: no_of_patrols
          aggregator: nunique
          column: patrol_id
        - display_name: distance_km
          aggregator: sum
          column: dist_meters
          original_unit: m
          new_unit: km
        - display_name: duration_hrs
          aggregator: sum
          column: timespan_seconds
          original_unit: s
          new_unit: h
        - display_name: average_speed 
          aggregator: mean 
          column: speed_kmhr
      reset_index: true
      df: ${{ workflow.rename_motor_trajs.return }}

  - name: Persist motorbike patrol metrics
    id: persist_motor_df
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.motor_patrol_metrics.return }}
      filename: motorbike_patrol_efforts

  - name: Create foot patrol coverage grid 
    id: foot_patrol_coverage
    task: create_patrol_coverage_grid 
    partial: 
      trajs: ${{ workflow.rename_foot_trajs.return }}
      aoi: ${{ workflow.filter_conservancy_boundary.return }}
      grid_cell_size: 1000 
      keep_empty_cells: false

  - name: Create vehicle patrol coverage grid 
    id: vehicle_patrol_coverage
    task: create_patrol_coverage_grid 
    partial: 
      trajs: ${{ workflow.rename_vehicle_trajs.return }}
      aoi: ${{ workflow.filter_conservancy_boundary.return }}
      grid_cell_size: 1000 
      keep_empty_cells: false

  - name: Create motor patrol coverage grid 
    id: motor_patrol_coverage
    task: create_patrol_coverage_grid 
    partial: 
      trajs: ${{ workflow.rename_motor_trajs.return }}
      aoi: ${{ workflow.filter_conservancy_boundary.return }}
      grid_cell_size: 1000 
      keep_empty_cells: false

  - name: Reproject foot patrols coverage grid to epsg 4326
    id: reproject_foot
    task: reproject_gdf 
    partial: 
      gdf: ${{ workflow.foot_patrol_coverage.return }}
      target_crs: "EPSG:4326"

  - name: Reproject vehicle patrols coverage grid to epsg 4326
    id: reproject_vehicle
    task: reproject_gdf 
    partial: 
      gdf: ${{ workflow.vehicle_patrol_coverage.return }}
      target_crs: "EPSG:4326"

  - name: Reproject motor patrols coverage grid to epsg 4326
    id: reproject_motor
    task: reproject_gdf 
    partial: 
      gdf: ${{ workflow.motor_patrol_coverage.return }}
      target_crs: "EPSG:4326"

  # Foot patrol map
  - name: Apply bin classification on foot patrol grids
    id: apply_foot_class_grid
    task: apply_classification
    partial:
      input_column_name: unique_patrol_count
      output_column_name: density_bins 
      label_options:
        label_ranges: false
        label_decimals: 1
      classification_options:
        k: 5
        scheme: "equal_interval"
      df: ${{ workflow.reproject_foot.return }}

  - name: Apply Colormap to foot patrol grid visits
    id: apply_foot_grid_colormap
    task: apply_color_map
    partial:
      input_column_name: density_bins
      colormap: RdYlGn
      output_column_name: density_colors
      df: ${{ workflow.apply_foot_class_grid.return }}

  - name: Create  foot patrol visit grid layers
    id: generate_foot_grid_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      layer_style: 
        filled: true 
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: density_colors
        get_line_color: [0, 0, 0]
        opacity: 0.35
        get_line_width: 0.95
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5
      legend:
        title: Grid Cell Visits
        label_column: density_bins
        color_column: density_colors
      geodataframe: ${{ workflow.apply_foot_grid_colormap.return }}
      data_url: null

  - name: Combine styled layers with foot patrol layer
    id: combine_foot_patrol
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_foot_grid_layer.return }}

  - name: Draw foot patrol map
    id: draw_foot_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_foot_patrol.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist foot patrol map HTML paths
    id: persist_foot_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_foot_map.return }}
      filename: foot_patrol_map.html

  - name: Convert foot patrol map to png
    id: convert_foot_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_foot_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Create foot patrol map widget
    id: foot_map_widget
    task: create_map_widget_single_view
    partial:
      title: "Foot Patrol Coverage Map"
      data: ${{ workflow.persist_foot_urls.return }}

  # Vehicle patrol map
  - name: Apply bin classification on vehicle patrol grids
    id: apply_vh_class_grid
    task: apply_classification
    partial:
      input_column_name: unique_patrol_count
      output_column_name: density_bins 
      label_options:
        label_ranges: false
        label_decimals: 1
      classification_options:
        k: 5
        scheme: "equal_interval"
      df: ${{ workflow.reproject_vehicle.return }}

  - name: Apply Colormap to vehicle patrol grid visits
    id: apply_vh_grid_colormap
    task: apply_color_map
    partial:
      input_column_name: density_bins
      colormap: RdYlGn
      output_column_name: density_colors
      df: ${{ workflow.apply_vh_class_grid.return }}

  - name: Create vehicle patrol visit grid layers
    id: generate_vh_grid_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      layer_style: 
        filled: true 
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: density_colors
        get_line_color: [0, 0, 0]
        opacity: 0.35
        get_line_width: 0.95
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5
      legend:
        title: Grid Cell Visits
        label_column: density_bins
        color_column: density_colors
      geodataframe: ${{ workflow.apply_vh_grid_colormap.return }}
      data_url: null

  - name: Combine styled layers with vehicle patrol layer
    id: combine_vh_patrol
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_vh_grid_layer.return }}

  - name: Draw vehicle patrol map
    id: draw_vh_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_vh_patrol.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist vehicle patrol map HTML paths
    id: persist_vh_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_vh_map.return }}
      filename: vehicle_patrol_map.html

  - name: Convert vehicle patrol map to png
    id: convert_vh_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_vh_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Create vehicle patrol map widget
    id: vh_map_widget
    task: create_map_widget_single_view
    partial:
      title: "Vehicle Patrol Coverage Map"
      data: ${{ workflow.persist_vh_urls.return }}

  # Motor patrol map
  - name: Apply bin classification on motor patrol grids
    id: apply_mr_class_grid
    task: apply_classification
    partial:
      input_column_name: unique_patrol_count
      output_column_name: density_bins 
      label_options:
        label_ranges: false
        label_decimals: 1
      classification_options:
        k: 5
        scheme: "equal_interval"
      df: ${{ workflow.reproject_motor.return }}

  - name: Apply Colormap to motor patrol grid visits
    id: apply_mr_grid_colormap
    task: apply_color_map
    partial:
      input_column_name: density_bins
      colormap: RdYlGn
      output_column_name: density_colors
      df: ${{ workflow.apply_mr_class_grid.return }}

  - name: Create motor patrol visit grid layers
    id: generate_mr_grid_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      layer_style: 
        filled: true 
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: density_colors
        get_line_color: [0, 0, 0]
        opacity: 0.35 
        get_line_width: 0.95
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5
      legend:
        title: Grid Cell Visits
        label_column: density_bins
        color_column: density_colors
      geodataframe: ${{ workflow.apply_mr_grid_colormap.return }}
      data_url: null

  - name: Combine styled layers with motor patrol layer
    id: combine_mr_patrol
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_mr_grid_layer.return }}

  - name: Draw motor patrol map
    id: draw_mr_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_mr_patrol.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist motor patrol map HTML paths
    id: persist_mr_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_mr_map.return }}
      filename: motor_patrol_map.html

  - name: Convert motor patrol map to png
    id: convert_mr_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_mr_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Create motor patrol map widget
    id: mr_map_widget
    task: create_map_widget_single_view
    partial:
      title: "Motorbike Patrol Coverage Map"
      data: ${{ workflow.persist_mr_urls.return }}

  #Overall patrols
  - name: Concatenate dataframes 
    id: concat_dataframes 
    task: concatenate_dataframes
    partial: 
      list_df: 
        - ${{ workflow.rename_foot_trajs.return }}
        - ${{ workflow.rename_vehicle_trajs.return }}
        - ${{ workflow.rename_motor_trajs.return }}
      axis: 0 
      join: "outer"
      ignore_index: true
      keys: null
      levels: null
      names: null
      verify_integrity: false
      sort: false
      copy: null

  - name: Create overall patrol coverage grid 
    id: overall_patrol_coverage
    task: create_patrol_coverage_grid 
    partial: 
      trajs: ${{ workflow.concat_dataframes.return }}
      aoi: ${{ workflow.filter_conservancy_boundary.return }}
      grid_cell_size: 1000 
      keep_empty_cells: false

  - name: Reproject overall patrols coverage grid to epsg 4326
    id: reproject_overall
    task: reproject_gdf 
    partial: 
      gdf: ${{ workflow.overall_patrol_coverage.return }}
      target_crs: "EPSG:4326"

  # Overall patrol map
  - name: Apply bin classification on overall patrol grids
    id: apply_ov_class_grid
    task: apply_classification
    partial:
      input_column_name: unique_patrol_count
      output_column_name: density_bins 
      label_options:
        label_ranges: false
        label_decimals: 1
      classification_options:
        k: 5
        scheme: "equal_interval"
      df: ${{ workflow.reproject_overall.return }}

  - name: Apply Colormap to overall patrol grid visits
    id: apply_ov_grid_colormap
    task: apply_color_map
    partial:
      input_column_name: density_bins
      colormap: RdYlGn
      output_column_name: density_colors
      df: ${{ workflow.apply_ov_class_grid.return }}

  - name: Create overall patrol visit grid layers
    id: generate_ov_grid_layer
    task: ecoscope_workflows_ext_custom.tasks.results.create_geojson_layer
    partial:
      layer_style: 
        filled: true 
        stroked: true 
        extruded: false 
        wireframe: false
        get_fill_color: density_colors
        get_line_color: [0, 0, 0]
        opacity: 0.35 
        get_line_width: 0.95
        get_elevation: 0
        get_point_radius: 1
        line_width_units: pixels 
        line_width_scale: 1 
        line_width_min_pixels: 1
        line_width_max_pixels: 5
      legend:
        title: Grid Cell Visits
        label_column: density_bins
        color_column: density_colors
      geodataframe: ${{ workflow.apply_ov_grid_colormap.return }}
      data_url: null

  - name: Combine styled layers with overall patrol layer
    id: combine_ov_patrol
    task: ecoscope_workflows_ext_ste.tasks.spatial_operations.combine_deckgl_map_layers
    partial:
      static_layers:
        - ${{ workflow.create_parcels_layer.return }}
        - ${{ workflow.create_conservancy_layer.return }}
      grouped_layers: ${{ workflow.generate_ov_grid_layer.return }}

  - name: Draw overall patrol map
    id: draw_ov_map
    task: ecoscope_workflows_ext_big_life.tasks.results.draw_map
    partial:
      tile_layers: ${{ workflow.configure_base_maps.return }}
      static: false
      title: null
      max_zoom: 10
      legend_style:
        placement: "bottom-right"
      geo_layers: ${{ workflow.combine_ov_patrol.return }}
      view_state: ${{ workflow.gdf_image_extent.return }}

  - name: Persist overall patrol map HTML paths
    id: persist_ov_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.draw_ov_map.return }}
      filename: overall_patrol_map.html

  - name: Convert overall patrol map to png
    id: convert_ov_png
    task: html_to_png
    partial:
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      html_path: ${{ workflow.persist_ov_urls.return }}
      config:
        full_page: false
        device_scale_factor: 2.0
        wait_for_timeout: 40000
        max_concurrent_pages: 1

  - name: Create overall patrol map widget
    id: ov_map_widget
    task: create_map_widget_single_view
    partial:
      title: "Overall Patrol Coverage Map"
      data: ${{ workflow.persist_ov_urls.return }}

  - name: Persist combined trajectories data
    id: persist_trajectories_data
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: geoparquet
      df: ${{ workflow.reproject_overall.return }}
      filename: patrol_trajectories

  - name: Summarize overall ranger patrol metrics
    id: ranger_patrol_metrics
    task: summarize_df
    partial:
      groupby_cols:
        - participants
      summary_params:
        - display_name: number_of_patrols
          aggregator: nunique
          column: patrol_id
          original_unit: null 
          new_unit: null
          decimal_places: 0 

        - display_name: distance_km
          aggregator: sum
          column: dist_meters
          original_unit: m
          new_unit: km
          decimal_places: 2

        - display_name: duration_hours
          aggregator: sum
          column: timespan_seconds
          original_unit: s
          new_unit: h
          decimal_places: 2

      reset_index: true
      df: ${{ workflow.concat_dataframes.return }}

  - name: Fill missing values with Undefined
    id: fill_participants
    task: fill_missing_values
    partial:
      df: ${{ workflow.ranger_patrol_metrics.return }}
      subset: 
        - participants
      value: Undefined
  
  - name: Convert number_of_patrols column to int 
    id: no_of_patrols_int 
    task: convert_columns_to_int 
    partial: 
      df: ${{ workflow.fill_participants.return }}
      columns: 
        - number_of_patrols 
      errors: coerce 
      fill_value: 0 

  - name: Persist total patrol coverage 
    id: persist_total_df
    task: persist_df
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.no_of_patrols_int.return }}
      filename: overall_patrol_efforts

  - name: Draw overall patrol efforts table
    id: patrol_efforts_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.no_of_patrols_int.return }}
      columns: null
      table_config: {enable_sorting: true, enable_filtering: true, enable_download: false, hide_header: false}
      widget_id: "Overall Patrol Efforts"

  - name: Save overall patrol efforts table (HTML)
    id: patrol_efforts_table_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.patrol_efforts_table_html.return }}
      filename: overall_patrol_efforts_table.html

  - name: Create overall patrol efforts table widget
    id: patrol_efforts_table_widget
    task: create_table_widget_single_view
    partial:
      title: "Overall Patrol Efforts"
      data: ${{ workflow.patrol_efforts_table_url.return }}

  - name: Reproject conservancy to epsg:3857
    id: reproject_conservancy
    task: reproject_gdf 
    partial: 
      gdf: ${{ workflow.filter_conservancy_boundary.return }}
      target_crs: "EPSG:3857"

  - name: Compute patrol occupancy
    id: compute_cons_occupancy
    task: compute_patrol_occupancy
    partial:  
      conservancies: ${{ workflow.reproject_conservancy.return }}
      patrol_coverage: ${{ workflow.overall_patrol_coverage.return }}
  
  - name: persist occupancy df 
    id: persist_occupancy_df
    task: persist_df
    partial: 
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      filetype: csv
      df: ${{ workflow.compute_cons_occupancy.return }}
      filename: patrol_coverage

  - name: Draw patrol occupancy table
    id: occupancy_table_html
    task: draw_table
    partial:
      dataframe: ${{ workflow.compute_cons_occupancy.return }}
      columns: null
      table_config: {enable_sorting: true, enable_filtering: true, enable_download: false, hide_header: false}
      widget_id: "Conservancy Patrol Occupancy"

  - name: Save patrol occupancy table (HTML)
    id: occupancy_table_url
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      text: ${{ workflow.occupancy_table_html.return }}
      filename: patrol_coverage_table.html

  - name: Create patrol occupancy table widget
    id: occupancy_table_widget
    task: create_table_widget_single_view
    partial:
      title: "Conservancy Patrol Occupancy"
      data: ${{ workflow.occupancy_table_url.return }}

  - name: Download MNC Template and persist
    id: fetch_mnc_template
    task: ecoscope_workflows_ext_ste.tasks.io.fetch_and_persist_file
    partial:
      url: https://www.dropbox.com/scl/fi/cd29du0tkel6k74cm96sy/mara_north_event_template.docx?rlkey=q6q01pvxnlfa146eix1ck640n&st=3menrr2d&dl=0
      output_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      overwrite_existing: false
      retries: 2
      unzip: false

  - name: Generate MNC overall report 
    id: generate_overall_report 
    task: generate_mnc_report 
    partial: 
      template_path: ${{ workflow.fetch_mnc_template.return }}
      output_dir: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      generated_by: "Ecoscope"
      validate_images: true
      time_period: ${{ workflow.time_range.return }}
      filename: "overall_report.docx"

  - name: MNC event report dashboard
    id: mnc_events_dashboard
    task: gather_dashboard
    partial:
      details: ${{ workflow.workflow_details.return}}
      widgets:
        # Weather Widgets
        - ${{ workflow.precipitation_widget.return }}
        - ${{ workflow.temperature_widget.return }}
        - ${{ workflow.wind_speed_widget.return }}
        - ${{ workflow.wind_gusts_widget.return }}
        - ${{ workflow.soil_temp_widget.return }}
        - ${{ workflow.rel_humidity_widget.return }}
        - ${{ workflow.pressure_widget.return }}

        # Logistics Widgets 
        - ${{ workflow.balloon_landing_table_widget.return }}
        - ${{ workflow.airstrip_op_table_widget.return }}
        - ${{ workflow.airstrip_maint_table_widget.return }}

        # Livestock Widgets 
        - ${{ workflow.mobile_boma_map_widget.return }}
        - ${{ workflow.livestock_predation_map_widget.return }}
        - ${{ workflow.illegal_grazing_map_widget.return }}
        - ${{ workflow.cattle_count_table_widget.return }}
        - ${{ workflow.predation_summary_table_widget.return }}

        # Wildlife Widgets 
        - ${{ workflow.wildlife_map_widget.return }}
        - ${{ workflow.ele_herd_size_map_widget.return }}
        - ${{ workflow.ele_herd_comp_map_widget.return }}
        - ${{ workflow.ele_bar_widget.return }}
        - ${{ workflow.ele_table_widget.return }}
        - ${{ workflow.buff_herd_size_map_widget.return }}
        - ${{ workflow.buff_herd_comp_map_widget.return }}
        - ${{ workflow.buff_bar_widget.return }}
        - ${{ workflow.buff_table_widget.return }}
        - ${{ workflow.lion_map_widget.return }}
        - ${{ workflow.lion_table_widget.return }}
        - ${{ workflow.leo_map_widget.return }}
        - ${{ workflow.leo_table_widget.return }}
        - ${{ workflow.chee_map_widget.return }}
        - ${{ workflow.chee_table_widget.return }}
        - ${{ workflow.gir_map_widget.return }}
        - ${{ workflow.gir_table_widget.return }}
        - ${{ workflow.hart_map_widget.return }}
        - ${{ workflow.hart_table_widget.return }}
        - ${{ workflow.rhi_map_widget.return }}
        - ${{ workflow.rhi_table_widget.return }}
      
        # Patrol Effort Widgets
        - ${{ workflow.foot_map_widget.return }}
        - ${{ workflow.vh_map_widget.return }}
        - ${{ workflow.mr_map_widget.return }}
        - ${{ workflow.ov_map_widget.return }}
        - ${{ workflow.events_chart_widget.return }}
        - ${{ workflow.patrol_summary_table_widget.return }}
        - ${{ workflow.patrol_efforts_table_widget.return }}
        - ${{ workflow.occupancy_table_widget.return }}
      time_range: ${{ workflow.time_range.return}}
      groupers: ${{ workflow.groupers.return }}
